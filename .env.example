# ===== Postgres =====
POSTGRES_DB=twochi
POSTGRES_USER=twochi
POSTGRES_PASSWORD=changeme
POSTGRES_PORT=5433   # 호스트에서 노출되는 포트. 호스트 Postgres 와 충돌 방지 (컨테이너 내부는 5432 유지).

# ===== Backend (Spring Boot 가 읽음) =====
DB_HOST=localhost
DB_PORT=5433
DB_NAME=twochi
DB_USER=twochi
DB_PASSWORD=changeme
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=local

# ===== MinIO =====
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# ===== Redis =====
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=changeme

# ===== JWT / Auth =====
JWT_SECRET=dev-only-change-me-min-32-bytes-please-1234
COOKIE_SECURE=false

# ===== Frontend =====
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# ===== OpenAI =====
OPENAI_API_KEY=

# ===== Cover Letter 암호화 (5.6) =====
# 마스터 자소서 답변 (cover_letter_master.master_answer) AES-256 암호화 키.
# 생성: openssl rand -hex 32  /  openssl rand -hex 8
# 운영에서 분실 시 기존 데이터 복호화 불가 — 백업 필수.
MASTER_ANSWER_ENCRYPTION_KEY=
MASTER_ANSWER_ENCRYPTION_SALT=
