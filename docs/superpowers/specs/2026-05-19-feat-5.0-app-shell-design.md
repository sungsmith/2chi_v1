# feat/5.0-app-shell — 인증 페이지용 공통 레이아웃 설계

작성일: 2026-05-19  
상태: 승인됨  
참조: `files/md/2chi_v1_fd_v0.3.md` §5.0 사이트맵, `design_system/pages/dashboard.{html,css,jsx}` 의 TopNav 부분

---

## 1. 범위

**포함:**
- **TopNav** — 로고 / 5메뉴 (대시보드·내 정보·지원 현황·이직·취업·기업) / 검색·알림 아이콘 버튼 (placeholder, 클릭 시 토스트) / ProfileMenu 드롭다운
- **ProfileMenu** — 닉네임 + 마이페이지 링크 + 로그아웃 액션
- **AppShellLayout** — Next.js route group `(app)/layout.tsx`, TopNav 포함 + 비로그인 시 `/login?from=...` 리다이렉트
- **라우트 그룹 재배치** — `(public)/` 와 `(app)/` 분리
- **로그인 후 from 파라미터 복귀** — LoginForm 가 `from` 쿼리로 돌아갈 페이지 인식
- **placeholder 페이지** — `/me`, `/applications`, `/cover-letter`, `/jobs` 의 WIP 안내 페이지
- **로고 파일** — `design_system/assets/logo.svg` → `frontend/public/logo.svg` 복사

**제외 (다음 브랜치):**
- 실제 대시보드 위젯 — `feat/5.3-dashboard`
- 알림 카운트 배지·목록 — `feat/5.8-notifications`
- 검색 기능 — 기능 미정의
- 마이페이지 실제 콘텐츠 — `feat/5.1-auth-mypage`
- 모바일 햄버거 메뉴 — v1 후속 또는 v2

---

## 2. 라우트 구조

```
frontend/src/app/
├── layout.tsx                  # 그대로 (AuthProvider)
├── globals.css                 # 그대로
├── (public)/
│   ├── login/page.tsx          # 이동
│   ├── signup/page.tsx         # 이동
│   ├── terms/page.tsx          # 이동
│   ├── privacy/page.tsx        # 이동
│   └── onboarding/page.tsx     # 이동 (단독 흐름, 자체 가드 유지)
└── (app)/
    ├── layout.tsx              # NEW: AppShellLayout
    ├── page.tsx                # 이동: 홈 placeholder + HomeBanner
    ├── me/page.tsx             # NEW: WIP
    ├── applications/page.tsx   # NEW: WIP
    ├── cover-letter/page.tsx   # NEW: WIP
    └── jobs/page.tsx           # NEW: WIP
```

route group `(name)` 은 URL 에 영향 없음 — `/(app)/me/page.tsx` 는 `/me` 로 라우팅.

---

## 3. AppShellLayout

`frontend/src/app/(app)/layout.tsx`:

```tsx
"use client";

import { useEffect } from "react";
import { useRouter, usePathname } from "next/navigation";
import { useAuth } from "@/contexts/auth-context";
import { TopNav } from "@/components/app-shell/top-nav";

export default function AppLayout({ children }: { children: React.ReactNode }) {
  const { user, initialized } = useAuth();
  const router = useRouter();
  const pathname = usePathname();

  useEffect(() => {
    if (!initialized) return;
    if (!user) router.replace(`/login?from=${encodeURIComponent(pathname)}`);
  }, [initialized, user, router, pathname]);

  if (!initialized) return null;
  if (!user) return null;

  return (
    <>
      <TopNav />
      <main style={{ background: "var(--color-bg)", minHeight: "calc(100vh - 64px)" }}>
        {children}
      </main>
    </>
  );
}
```

---

## 4. TopNav 구성

### 4.1 TopNav

`frontend/src/components/app-shell/top-nav.tsx` — 64px sticky header, max-width 1280px, gap 토큰 사용. 좌측 로고 / 가운데 NavMenu / 우측 검색·알림·ProfileMenu.

### 4.2 NavMenu

`frontend/src/components/app-shell/nav-menu.tsx`:

5개 메뉴 — 경로 매칭으로 active 상태 결정. active 메뉴는 `--color-text-brand` 색 + 하단 2px indicator (`--color-primary-500`).

```ts
const MENUS = [
  { href: "/", label: "대시보드" },
  { href: "/me", label: "내 정보" },
  { href: "/applications", label: "지원 현황" },
  { href: "/cover-letter", label: "이직 / 취업" },
  { href: "/jobs", label: "기업" },
];
```

active 판정: `/` 는 정확 매칭, 나머지는 `pathname.startsWith(href)`.

### 4.3 ProfileMenu

`frontend/src/components/app-shell/profile-menu.tsx`:

- 트리거 버튼: 28px 원형 이니셜 아바타 (닉네임 첫 글자) + 닉네임
- 드롭다운: minWidth 180px, `--shadow-floating`, "마이페이지" / "로그아웃" 2개 아이템
- 외부 클릭 시 닫힘 (`useRef` + `mousedown` 리스너)
- 로그아웃 → `useAuth().logout()` → `router.push("/login")`
- `user === null` 이면 렌더링 안 함

### 4.4 NavIconButton (공용)

`frontend/src/components/app-shell/nav-icon-button.tsx` — 36×36 정사각 transparent 버튼, hover 색 토큰. props: `ariaLabel`, `onClick`, `children`.

### 4.5 검색·알림 버튼

TopNav 내부에서 NavIconButton 사용. SVG 아이콘 inline (lucide-style stroke 1.8). 클릭 시 `alert()` 로 "검색/알림 기능은 곧 제공됩니다." 안내. **빨간 카운트 배지는 알림 도메인 도입 시 추가**, 이번 브랜치 제외.

---

## 5. LoginForm 변경 — from 파라미터

기존 로그인 성공 시 onboardingCompleted 기준 분기. 새: `from` 쿼리 파라미터 우선 (단, `/` 로 시작하는 안전한 경로만).

```ts
const searchParams = useSearchParams();
// ...
const u = await login(email, password);
const from = searchParams.get("from");
const dest = u.onboardingCompleted
  ? (from && from.startsWith("/") ? from : "/")
  : "/onboarding";
router.push(dest);
```

비로그인 사용자가 `/me` 접근 → `/login?from=%2Fme` → 로그인 성공 (onboarding 완료자) → `/me` 로 복귀.

---

## 6. placeholder 페이지 (4개)

`(app)/me/page.tsx`, `(app)/applications/page.tsx`, `(app)/cover-letter/page.tsx`, `(app)/jobs/page.tsx` — 동일 패턴:

```tsx
export default function MePage() {
  return (
    <section style={{ maxWidth: 720, margin: "0 auto", padding: "var(--space-12) var(--space-6)" }}>
      <h1 style={{ fontFamily: "var(--font-family-hand)", fontSize: "var(--fs-page-title)" }}>
        내 정보
      </h1>
      <p style={{ color: "var(--color-text-secondary)", marginTop: "var(--space-4)" }}>
        이 기능은 다음 단계에 구현됩니다.
      </p>
    </section>
  );
}
```

각 페이지 제목·문구만 차별화. metadata 도 각자 (`title: "내 정보 · 2chi"` 등).

---

## 7. 로고 파일

`design_system/assets/logo.svg` 를 `frontend/public/logo.svg` 로 복사. Next.js `public/` 디렉토리에서 `/logo.svg` 로 절대 경로 참조.

---

## 8. 테스트

### 8.1 `top-nav.test.tsx`

| 케이스 | 검증 |
|---|---|
| user=null 시 ProfileMenu 미렌더 | 닉네임/메뉴 없음 |
| 로그인 사용자 닉네임 표시 | "alice" 텍스트 존재 |
| ProfileMenu 클릭 → 드롭다운 열림 | "마이페이지", "로그아웃" 보임 |
| 외부 클릭 → 드롭다운 닫힘 | 메뉴 사라짐 |
| "로그아웃" 클릭 → logout + /login 이동 | mock 호출 검증 |
| pathname="/me" 시 "내 정보" active | indicator 보임 |

### 8.2 `(app)/layout.test.tsx`

| 케이스 | 검증 |
|---|---|
| 비로그인 → /login?from=... 리다이렉트 | router.replace 호출 |
| 인증된 사용자 → 자식 렌더 | children 보임 |
| 초기화 전 → null (깜빡임 방지) | render result 비어있음 |

### 8.3 기존 테스트 회귀

- signup-form, login-form, onboarding-flow — 라우트 그룹 이동은 URL 영향 없음, mock 변경 없음 → 무영향
- login-form 에 from 분기 추가 → 기존 케이스 영향 없음 (from 미존재 시 기존 분기 동작)

---

## 9. 파일 책임 분리 (라인 수)

| 파일 | 라인 |
|---|---|
| `(app)/layout.tsx` | ~30 |
| `components/app-shell/top-nav.tsx` | ~80 |
| `components/app-shell/nav-menu.tsx` | ~50 |
| `components/app-shell/profile-menu.tsx` | ~90 |
| `components/app-shell/nav-icon-button.tsx` | ~30 |
| placeholder 페이지 4개 | 각 ~25 |
| `top-nav.test.tsx` | ~120 |
| `(app)/layout.test.tsx` | ~50 |
| LoginForm `from` 분기 | +15 |

각 80~120 라인.

---

## 10. 다음 단계

이 설계로 `writing-plans` 호출 → `docs/superpowers/plans/2026-05-19-feat-5.0-app-shell.md`. 구현 완료 후 PR merge → `feat/5.3-dashboard` 재개.
