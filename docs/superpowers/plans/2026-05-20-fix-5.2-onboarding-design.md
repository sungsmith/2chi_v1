# fix/5.2-onboarding-design Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 5.2 의 `/onboarding` 페이지 UI 를 시안 `design_system/pages/onboarding.{html,css,jsx}` 와 일치시킴 (BE 무변경). 좌측 BrandPanel + 우측 폼 2-컬럼 스테이지, Welcome modal, 시안 카드/chip 시각.

**Architecture:** 5.3-dashboard 와 동일 패턴 — 시안 CSS 페이지 단위 import + 클래스 기반 JSX. `(public)/onboarding/page.tsx` 의 인증 가드 유지, `OnboardingFlow` 의 데이터 로직 (useState + postOnboarding) 유지, JSX 구조와 컴포넌트들만 재작성. Welcome modal 이 제출 성공 → `/` 이동을 인터셉트.

**Tech Stack:** Next.js (app router) / React / TypeScript / Vitest + @testing-library/react / 기존 AuthContext / 기존 onboarding API.

**관련 문서:**
- 설계: [docs/superpowers/specs/2026-05-20-fix-5.2-onboarding-design.md](../specs/2026-05-20-fix-5.2-onboarding-design.md)
- 시안: `design_system/pages/onboarding.html` / `onboarding.css` / `onboarding.jsx`
- 패턴 참조: 5.3-dashboard 의 같은 분할 패턴 ([docs/superpowers/plans/2026-05-20-feat-5.3-dashboard.md](2026-05-20-feat-5.3-dashboard.md))
- 프론트 주의: `frontend/AGENTS.md` — "Next.js you know 아니다" 경고. 본 플랜은 기존 사용 API 만 사용

---

## 파일 맵

| 파일 | 책임 | 생성/수정 |
|---|---|---|
| `frontend/src/app/(public)/onboarding/page.tsx` | 인증 가드 + `<main className="onb-stage">` + `<OnboardingFlow />` | 수정 |
| `frontend/src/app/(public)/onboarding/onboarding.css` | 시안 onboarding.css 변환 결과 (body→.onb-stage) | 생성 |
| `frontend/src/components/onboarding/onboarding-flow.tsx` | 전체 조립 ("use client") — 시안 App() 구조 + 5.2 데이터 로직 유지 + WelcomeModal 게이트 | 재작성 |
| `frontend/src/components/onboarding/brand-panel.tsx` | 좌측 `.onb-left` 패널 (로고+헤드라인+tip-card) | 생성 |
| `frontend/src/components/onboarding/stepper.tsx` | 시안 `.onb-stepper` (done/active/숫자+체크) | 재작성 |
| `frontend/src/components/onboarding/step-purpose.tsx` | Step1 — 2 cards (취업/이직) | 재작성 |
| `frontend/src/components/onboarding/step-career.tsx` | Step2 — 8 chips (신입~7+) | 재작성 |
| `frontend/src/components/onboarding/step-positions.tsx` | Step3 — 5 cards (FE/BE/Infra-Cloud/Infra-Ops/UI-UX) | 재작성 |
| `frontend/src/components/onboarding/step-recap.tsx` | Step4 — recap 3 row + sample-grid 3 cards | 재작성 |
| `frontend/src/components/onboarding/welcome-modal.tsx` | 제출 성공 후 `.onb-welcome` modal | 생성 |
| `frontend/src/components/onboarding/icons.tsx` | 시안 인라인 SVG (사용분 ~15개) | 생성 |
| `frontend/src/components/onboarding/__tests__/onboarding-flow.test.tsx` | selector 조정 + WelcomeModal step 추가 | 수정 |

---

## Task 1: onboarding.css 복사·변환 + page.tsx 임시 셸

**목표:** 시안 CSS 가 페이지에 로드되고 `<main className="onb-stage">` 컨테이너가 활성화. 위젯 없이 빌드/회귀 통과 확인.

**Files:**
- Create: `frontend/src/app/(public)/onboarding/onboarding.css`
- Modify: `frontend/src/app/(public)/onboarding/page.tsx`

- [ ] **Step 1: onboarding.css 복사**

```bash
cp /Users/sungjiwon/claude/2chi_v1/design_system/pages/onboarding.css /Users/sungjiwon/claude/2chi_v1/frontend/src/app/\(public\)/onboarding/onboarding.css
```

- [ ] **Step 2: 변환 — `body.onb-body` → `.onb-stage`**

원본 L5~13 의 `body.onb-body { ... }` 블록과 L18~45 의 `body.onb-body::before/::after` 블록을 아래로 교체.

**Before (원본):**
```css
body.onb-body {
  margin: 0;
  background: var(--color-bg);
  min-height: 100vh;
  font-family: var(--font-family-sans);
  color: var(--color-text-primary);
  overflow-x: hidden;
  position: relative;
}

/* ============================================================
   Ambient background — soft animated blobs
============================================================ */
body.onb-body::before,
body.onb-body::after {
  content: "";
  position: fixed;
  pointer-events: none;
  z-index: 0;
  filter: blur(80px);
  will-change: transform;
}
body.onb-body::before {
  top: -280px; right: -260px;
  width: 880px; height: 880px;
  background:
    radial-gradient(closest-side, var(--color-lavender-200), transparent 70%),
    radial-gradient(closest-side at 30% 70%, var(--color-primary-100), transparent 75%);
  opacity: 0.55;
  animation: onbBlobA 28s ease-in-out infinite alternate;
}
body.onb-body::after {
  bottom: -340px; left: -300px;
  width: 1020px; height: 1020px;
  background:
    radial-gradient(closest-side, var(--color-mint-200), transparent 70%),
    radial-gradient(closest-side at 30% 70%, var(--color-peach-100), transparent 75%),
    radial-gradient(closest-side at 80% 25%, var(--color-pink-100), transparent 75%);
  opacity: 0.5;
  animation: onbBlobB 34s ease-in-out infinite alternate;
}
```

**After:**
```css
.onb-stage {
  position: relative;
  overflow-x: hidden;
  min-height: 100vh;
}

/* ============================================================
   Ambient background — soft animated blobs
   position: fixed intentional — creates viewport-attached ambient wash.
   IMPORTANT: any transform/filter on an ancestor (.onb-stage or its parents)
   will confine these to that element. Verify after Task 7 visual check.
============================================================ */
.onb-stage::before,
.onb-stage::after {
  content: "";
  position: fixed;
  pointer-events: none;
  z-index: 0;
  filter: blur(80px);
  will-change: transform;
}
.onb-stage::before {
  top: -280px; right: -260px;
  width: 880px; height: 880px;
  background:
    radial-gradient(closest-side, var(--color-lavender-200), transparent 70%),
    radial-gradient(closest-side at 30% 70%, var(--color-primary-100), transparent 75%);
  opacity: 0.55;
  animation: onbBlobA 28s ease-in-out infinite alternate;
}
.onb-stage::after {
  bottom: -340px; left: -300px;
  width: 1020px; height: 1020px;
  background:
    radial-gradient(closest-side, var(--color-mint-200), transparent 70%),
    radial-gradient(closest-side at 30% 70%, var(--color-peach-100), transparent 75%),
    radial-gradient(closest-side at 80% 25%, var(--color-pink-100), transparent 75%);
  opacity: 0.5;
  animation: onbBlobB 34s ease-in-out infinite alternate;
}
```

(`margin/background/font-family/color` 제거 — globals.css 가 담당. `min-height: 100vh` 유지 — (public)/layout 에 `<main>` wrapper 없음.)

확인:
```bash
grep -nE "body\.onb-body|onb-body" /Users/sungjiwon/claude/2chi_v1/frontend/src/app/\(public\)/onboarding/onboarding.css
```
Expected: 출력 없음.

- [ ] **Step 3: dashboard.css 복사 후 executable bit 제거**

5.3 review 에서 동일 이슈 — `cp` 가 실행 비트를 가져옴.

```bash
cd /Users/sungjiwon/claude/2chi_v1
git update-index --chmod=-x "frontend/src/app/(public)/onboarding/onboarding.css" 2>/dev/null
chmod -x "frontend/src/app/(public)/onboarding/onboarding.css"
ls -la "frontend/src/app/(public)/onboarding/onboarding.css"
```
Expected: 권한이 `rw-r--r--` (실행 비트 없음).

- [ ] **Step 4: page.tsx 임시 셸로 수정**

`frontend/src/app/(public)/onboarding/page.tsx` 전체 교체:

```tsx
"use client";

import "./onboarding.css";
import { useEffect } from "react";
import { useRouter } from "next/navigation";
import { useAuth } from "@/contexts/auth-context";
import { OnboardingFlow } from "@/components/onboarding/onboarding-flow";

export default function OnboardingPage() {
  const { user, initialized } = useAuth();
  const router = useRouter();

  useEffect(() => {
    if (!initialized) return;
    if (!user) { router.replace("/login"); return; }
    if (user.onboardingCompleted) router.replace("/");
  }, [initialized, user, router]);

  if (!initialized || !user || user.onboardingCompleted) return null;

  return (
    <main className="onb-stage">
      <OnboardingFlow />
    </main>
  );
}
```

(직전 `<main style={{ maxWidth: 720, ... }}>` 가 `<main className="onb-stage">` 로 교체. import 1개 추가.)

- [ ] **Step 5: 빌드 + 테스트 회귀**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
rm -rf .next/dev
npm run build 2>&1 | tail -10
npm test 2>&1 | tail -10
```

Expected:
- BUILD SUCCESSFUL
- 5.2 onboarding-flow 테스트는 아직 OnboardingFlow 가 그대로라 PASS (37 PASS, 변화 없음)

이 단계에선 OnboardingFlow 자체는 미수정이라 시각 차이가 거의 없음 (`.onb-stage` 의 ambient blob 만 추가됨). 본격 시각 변경은 Task 2~ 부터.

- [ ] **Step 6: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add "frontend/src/app/(public)/onboarding/onboarding.css" "frontend/src/app/(public)/onboarding/page.tsx"
git commit -m "fix(onboarding): onboarding.css 복사·변환 + page.tsx 셸

- 시안 onboarding.css 를 (public)/onboarding/onboarding.css 로 복사
- body.onb-body → .onb-stage 래퍼 셀렉터 (페이지 단위 스코프)
- margin/background/font-family/color 중복 제거 (globals.css 담당)
- ambient blob 의 position:fixed risk 주석 추가
- page.tsx 의 main wrapper 를 .onb-stage 로 교체 + onboarding.css import"
```

---

## Task 2: icons.tsx (시안 인라인 SVG)

**목표:** 시안 dashboard 외 onboarding 전용 추가 아이콘 모음.

**Files:**
- Create: `frontend/src/components/onboarding/icons.tsx`

- [ ] **Step 1: icons.tsx 작성**

`frontend/src/components/onboarding/icons.tsx`:

```tsx
type IconProps = { size?: number; className?: string };

export function Briefcase({ size = 22, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <rect x="2" y="6" width="20" height="14" rx="2" />
      <path d="M16 6V4a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2" />
      <path d="M2 13h20" />
    </svg>
  );
}

export function Move({ size = 22, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <path d="M4 17a8 8 0 0 1 14-5.3" />
      <path d="M14 8h4v4" />
      <path d="M20 7a8 8 0 0 1-14 5.3" />
      <path d="M10 16H6v-4" />
    </svg>
  );
}

export function Code({ size = 22, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <polyline points="16 18 22 12 16 6" />
      <polyline points="8 6 2 12 8 18" />
    </svg>
  );
}

export function Server({ size = 22, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <rect x="3" y="3" width="18" height="7" rx="2" />
      <rect x="3" y="14" width="18" height="7" rx="2" />
      <line x1="7" y1="6.5" x2="7.01" y2="6.5" />
      <line x1="7" y1="17.5" x2="7.01" y2="17.5" />
    </svg>
  );
}

export function Cloud({ size = 22, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <path d="M17.5 19a4.5 4.5 0 0 0 .5-9 6 6 0 0 0-11.6 1.4A4 4 0 0 0 7 19z" />
    </svg>
  );
}

export function Gear({ size = 22, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <circle cx="12" cy="12" r="3" />
      <path d="M19.4 15a1.7 1.7 0 0 0 .3 1.8l.1.1a2 2 0 1 1-2.8 2.8l-.1-.1a1.7 1.7 0 0 0-1.8-.3 1.7 1.7 0 0 0-1 1.5V21a2 2 0 1 1-4 0v-.1a1.7 1.7 0 0 0-1.1-1.5 1.7 1.7 0 0 0-1.8.3l-.1.1a2 2 0 1 1-2.8-2.8l.1-.1a1.7 1.7 0 0 0 .3-1.8 1.7 1.7 0 0 0-1.5-1H3a2 2 0 1 1 0-4h.1a1.7 1.7 0 0 0 1.5-1.1 1.7 1.7 0 0 0-.3-1.8l-.1-.1a2 2 0 1 1 2.8-2.8l.1.1a1.7 1.7 0 0 0 1.8.3H9a1.7 1.7 0 0 0 1-1.5V3a2 2 0 1 1 4 0v.1a1.7 1.7 0 0 0 1 1.5 1.7 1.7 0 0 0 1.8-.3l.1-.1a2 2 0 1 1 2.8 2.8l-.1.1a1.7 1.7 0 0 0-.3 1.8V9a1.7 1.7 0 0 0 1.5 1H21a2 2 0 1 1 0 4h-.1a1.7 1.7 0 0 0-1.5 1z" />
    </svg>
  );
}

export function Layout({ size = 22, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <rect x="3" y="3" width="18" height="18" rx="2" />
      <path d="M3 9h18" />
      <path d="M9 21V9" />
    </svg>
  );
}

export function Check({ size = 14, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="2.4" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <polyline points="20 6 9 17 4 12" />
    </svg>
  );
}

export function Arrow({ dir, size = 14, className }: IconProps & { dir: "left" | "right" }) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <line x1="5" y1="12" x2={dir === "left" ? "11" : "19"} y2="12" />
      <polyline points={dir === "left" ? "11 6 5 12 11 18" : "13 6 19 12 13 18"} />
    </svg>
  );
}

export function Sparkle({ size = 16, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.6" strokeLinejoin="round" className={className}>
      <path d="M12 3l1.6 4.6L18 9l-4.4 1.4L12 15l-1.6-4.6L6 9l4.4-1.4z" />
      <path d="M19 3v3M21 5h-3M5 18v3M7 21H4" />
    </svg>
  );
}

export function NotebookPen({ size = 22, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <path d="M2 6a2 2 0 0 1 2-2h5l2 3h7a2 2 0 0 1 2 2v9a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2z" />
      <path d="M12 11v6M9 14h6" />
    </svg>
  );
}

export function FileText({ size = 22, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
      <polyline points="14 2 14 8 20 8" />
      <line x1="9" y1="13" x2="15" y2="13" />
      <line x1="9" y1="17" x2="13" y2="17" />
    </svg>
  );
}

export function Edit({ size = 22, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
      <polyline points="14 2 14 8 20 8" />
      <path d="M10.5 17.5l-2 .5.5-2 5-5 1.5 1.5z" />
    </svg>
  );
}
```

(시안 onboarding.jsx 의 Compass/Folder/Layers/Chart/Dots/Calendar 는 portfolio/explore/data/etc 제외 + Step4 sample 의 Calendar 미사용으로 추가하지 않음.)

- [ ] **Step 2: 타입체크**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npx tsc --noEmit 2>&1 | tail -5
```

Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/onboarding/icons.tsx
git commit -m "feat(onboarding): 시안 인라인 SVG 아이콘 13종 (icons.tsx)

Briefcase/Move (Step1) + Code/Server/Cloud/Gear/Layout (Step3) +
Check (선택 배지) + Arrow(left/right, 푸터 버튼) + Sparkle (Step2 노트) +
NotebookPen/FileText/Edit (Step4 sample-grid)"
```

---

## Task 3: BrandPanel + Stepper

**목표:** 좌측 정적 패널 + Stepper 재작성.

**Files:**
- Create: `frontend/src/components/onboarding/brand-panel.tsx`
- Modify: `frontend/src/components/onboarding/stepper.tsx`

- [ ] **Step 1: brand-panel.tsx 작성**

`frontend/src/components/onboarding/brand-panel.tsx`:

```tsx
export function BrandPanel() {
  return (
    <aside className="onb-left">
      <a className="onb-logo-mark" href="#" aria-label="이취 (2chi)">
        <img src="/logo.svg" alt="이취" style={{ height: 56, width: "auto", display: "block" }} />
      </a>

      <div className="headline">
        <div className="onb-eyebrow mono">welcome · 환영합니다</div>
        <h1>
          매번 다시 쓰지 않는<br />
          자소서, <span className="accent">한곳에서</span><br />
          정리되는 지원 흐름.
        </h1>
        <p className="lead">
          몇 가지만 알려주시면,<br />
          자소서와 지원 관리를 더 정확하게 도와드릴 수 있어요.
        </p>

        <div className="onb-tip-card">
          <span className="tape lav" />
          <div className="tip-icon">
            <span className="mascot-cloud sm">
              <span className="blush" />
            </span>
          </div>
          <div>
            <h4>4단계, 약 1분이면 끝나요</h4>
            <p>
              지금 답하지 않아도 괜찮아요. 언제든 <b>내 정보</b>에서 다시 다듬을 수 있어요.
            </p>
          </div>
        </div>
      </div>

      <div className="onb-foot-meta mono">v1 · 2026 Q2  ·  PC Web First</div>
    </aside>
  );
}
```

- [ ] **Step 2: stepper.tsx 재작성**

`frontend/src/components/onboarding/stepper.tsx` 전체 교체:

```tsx
import { Check } from "./icons";

const STEPS = [
  { num: 1, label: "준비 단계" },
  { num: 2, label: "경력 연차" },
  { num: 3, label: "희망 직무" },
  { num: 4, label: "둘러보기" },
];

type Props = { current: 1 | 2 | 3 | 4 };

export function Stepper({ current }: Props) {
  return (
    <div className="onb-stepper">
      {STEPS.map((s, i) => {
        const idx = i + 1;
        const state = idx < current ? "done" : idx === current ? "active" : "";
        return (
          <div key={s.num} className={`onb-step-pill ${state}`}>
            <div className="bar" />
            <div className="meta">
              <span className="num">{idx < current ? <Check size={10} /> : s.num}</span>
              <span>{s.label}</span>
            </div>
          </div>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 3: 타입체크 + 빌드**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npx tsc --noEmit 2>&1 | tail -5
npm run build 2>&1 | tail -8
```

Expected: 에러 없음.

- [ ] **Step 4: 회귀 확인**

```bash
npm test 2>&1 | tail -10
```

Expected: 37 PASS (이전과 동일 — Stepper 의 prop 시그니처가 같음).

만약 Stepper 의 JSX 변경으로 인해 5.2 의 어떤 테스트가 실패하면 그 케이스의 selector 를 Task 8 에서 조정. 본 Task 에서는 회귀 확인만 — 실패 시 메시지를 보고에 명시.

- [ ] **Step 5: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/onboarding/brand-panel.tsx frontend/src/components/onboarding/stepper.tsx
git commit -m "feat(onboarding): BrandPanel + Stepper 재작성

- BrandPanel: 좌측 .onb-left 패널 (로고+헤드라인+tip-card 정적)
- Stepper: 시안 .onb-stepper 4-pill (done/active + 체크 아이콘) 재구성
  prop 시그니처(current: 1|2|3|4) 무변경"
```

---

## Task 4: Step1/Step2/Step3 재작성 (3개 위젯)

**목표:** 시안 매핑된 카드·chip UI 로 3개 step 위젯 재작성. props 시그니처 무변경.

**Files:**
- Modify: `frontend/src/components/onboarding/step-purpose.tsx`
- Modify: `frontend/src/components/onboarding/step-career.tsx`
- Modify: `frontend/src/components/onboarding/step-positions.tsx`

- [ ] **Step 1: step-purpose.tsx 재작성**

`frontend/src/components/onboarding/step-purpose.tsx` 전체 교체:

```tsx
"use client";

import type { ReactNode } from "react";
import { Target } from "@/lib/enums/target";
import { Briefcase, Move, Check } from "./icons";

type Item = { target: Target; title: string; desc: string; icon: ReactNode; tone: string };

const ITEMS: Item[] = [
  { target: "EMPLOYMENT", title: "취업 준비 중", desc: "첫 직장을 차분히 찾고 있어요", icon: <Briefcase />, tone: "primary" },
  { target: "JOB_CHANGE", title: "이직 준비 중", desc: "더 잘 맞는 곳으로 옮기려 해요", icon: <Move />,      tone: "mint" },
];

type Props = { value: Target | undefined; onChange: (v: Target) => void };

export function StepPurpose({ value, onChange }: Props) {
  return (
    <div className="onb-scene entering">
      <div className="eyebrow">STEP 1 / 4</div>
      <h2>지금 어떤 준비를 하고 있나요?</h2>
      <p className="sub">처음이라도 괜찮아요. 이취가 준비 흐름을 같이 정리해드릴게요.</p>
      <div className="onb-choice-grid cols-2">
        {ITEMS.map((it) => {
          const selected = value === it.target;
          return (
            <button
              key={it.target}
              type="button"
              aria-pressed={selected}
              className={`onb-choice${selected ? " selected" : ""}`}
              onClick={() => onChange(it.target)}
            >
              <div className="icon-wrap" data-tone={it.tone}>{it.icon}</div>
              <div>
                <div className="title">{it.title}</div>
                <div className="desc">{it.desc}</div>
              </div>
              <span className="check"><Check size={12} /></span>
            </button>
          );
        })}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: step-career.tsx 재작성**

`frontend/src/components/onboarding/step-career.tsx` 전체 교체:

```tsx
"use client";

import { Sparkle } from "./icons";

const CAREERS = [
  { value: 0, num: "신입", lbl: "0년" },
  { value: 1, num: "1",   lbl: "년차" },
  { value: 2, num: "2",   lbl: "년차" },
  { value: 3, num: "3",   lbl: "년차" },
  { value: 4, num: "4",   lbl: "년차" },
  { value: 5, num: "5",   lbl: "년차" },
  { value: 6, num: "6",   lbl: "년차" },
  { value: 7, num: "7+",  lbl: "년차 이상" },
];

type Props = { value: number | undefined; onChange: (v: number) => void };

export function StepCareer({ value, onChange }: Props) {
  return (
    <div className="onb-scene entering">
      <div className="eyebrow">STEP 2 / 4</div>
      <h2>경력을 알려주세요</h2>
      <p className="sub">맞춤 자소서 흐름과 추천 공고 범위를 정하는 데 사용돼요. 나중에 언제든 바꿀 수 있어요.</p>
      <div className="career-grid">
        {CAREERS.map((c) => {
          const selected = value === c.value;
          return (
            <button
              key={c.value}
              type="button"
              aria-pressed={selected}
              className={`career-chip${selected ? " selected" : ""}`}
              onClick={() => onChange(c.value)}
            >
              <div className="num">{c.num}</div>
              <div className="lbl">{c.lbl}</div>
            </button>
          );
        })}
      </div>
      <div className="career-note">
        <Sparkle size={14} />
        <span>경력을 선택하면, 신입 / 주니어 / 시니어에 맞는 자소서 톤이 자동으로 조정돼요.</span>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: step-positions.tsx 재작성**

`frontend/src/components/onboarding/step-positions.tsx` 전체 교체:

```tsx
"use client";

import type { ReactNode } from "react";
import { TargetJob } from "@/lib/enums/target-job";
import { Code, Server, Cloud, Gear, Layout, Check } from "./icons";

type Item = { job: TargetJob; name: string; desc: string; icon: ReactNode; tone: number };

const ITEMS: Item[] = [
  { job: "FRONTEND",    name: "Frontend",      desc: "웹·앱 화면 구현",                       icon: <Code />,   tone: 1 },
  { job: "BACKEND",     name: "Backend",       desc: "서버·API·DB",                          icon: <Server />, tone: 1 },
  { job: "INFRA_CLOUD", name: "Infra / Cloud", desc: "AWS · GCP · K8s",                       icon: <Cloud />,  tone: 3 },
  { job: "INFRA_OPS",   name: "Infra / Ops",   desc: "VMware·NAS·백신·백업·네트워크",         icon: <Gear />,   tone: 3 },
  { job: "UI_UX",       name: "UI / UX",       desc: "제품·서비스 디자인",                     icon: <Layout />, tone: 2 },
];

type Props = { value: Set<TargetJob>; onToggle: (j: TargetJob) => void };

export function StepPositions({ value, onToggle }: Props) {
  return (
    <div className="onb-scene entering">
      <div className="eyebrow">STEP 3 / 4</div>
      <h2>어떤 직무를 준비하고 있나요?</h2>
      <p className="sub">여러 개 선택해도 괜찮아요. 선택한 직무를 기준으로 자소서, 기업분석, 채용공고가 맞춤화돼요.</p>
      <div className="position-grid">
        {ITEMS.map((it) => {
          const selected = value.has(it.job);
          return (
            <button
              key={it.job}
              type="button"
              aria-pressed={selected}
              className={`position-card tone-${it.tone}${selected ? " selected" : ""}`}
              onClick={() => onToggle(it.job)}
            >
              <div className="ico">{it.icon}</div>
              <div>
                <div className="name">{it.name}</div>
                <div className="desc">{it.desc}</div>
              </div>
              <span className="check"><Check size={11} /></span>
            </button>
          );
        })}
      </div>
      <div className="position-foot">
        <span className="badge info dot">선택됨</span>
        <span className="count">{value.size}개 직무</span>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: 타입체크 + 빌드**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npx tsc --noEmit 2>&1 | tail -5
npm run build 2>&1 | tail -8
```

Expected: 에러 없음.

- [ ] **Step 5: 회귀 확인**

```bash
npm test 2>&1 | tail -15
```

Expected 또는 일부 실패 OK:
- `name: /이직 준비 중/` 매칭 — `<div className="title">이직 준비 중</div>` 가 button 의 accessible name 에 포함 → PASS 가능
- `name: "3 년차"` 매칭 — `<div className="num">3</div><div className="lbl">년차</div>` → accessible name "3 년차" → PASS 가능
- `name: /Backend/` 매칭 — `<div className="name">Backend</div>` → PASS 가능
- `name: /Infra \/ Cloud/` — accessible name "Infra / Cloud" → PASS

만약 일부 실패하면 메시지 보고하고 다음 Task 5/6 진행 후 Task 8 에서 일괄 조정. 본 Task 에선 새 JSX 가 의도대로 렌더되는지만 확인.

- [ ] **Step 6: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/onboarding/step-purpose.tsx frontend/src/components/onboarding/step-career.tsx frontend/src/components/onboarding/step-positions.tsx
git commit -m "feat(onboarding): Step1/Step2/Step3 시안 매핑 재작성

- StepPurpose: 2 cards (취업/이직) + onb-choice-grid.cols-2 + aria-pressed
- StepCareer: 8 career-chip (신입/1~6/7+) + career-note (Sparkle)
- StepPositions: 5 position-card (FE/BE/Infra-Cloud/Infra-Ops/UI-UX) + position-foot
- props 시그니처 무변경 — onboarding-flow 호출부 영향 없음"
```

---

## Task 5: Step4 재작성 (StepRecap)

**목표:** Step4 가 recap 3-row + sample-grid 3 카드 렌더.

**Files:**
- Modify: `frontend/src/components/onboarding/step-recap.tsx`

- [ ] **Step 1: step-recap.tsx 재작성**

`frontend/src/components/onboarding/step-recap.tsx` 전체 교체:

```tsx
"use client";

import { Target, TARGET_META } from "@/lib/enums/target";
import { TargetJob, TARGET_JOB_META } from "@/lib/enums/target-job";
import { NotebookPen, FileText, Edit } from "./icons";

const SAMPLES = [
  { kbd: "01", title: "내 경험 정리",         desc: "프로젝트·성과를 PRAR 구조로 차곡차곡", icon: <NotebookPen />, tone: 1 },
  { kbd: "02", title: "자소서 초안 생성",     desc: "정리된 경험으로 마스터 자소서 한 벌",  icon: <FileText />,    tone: 2 },
  { kbd: "03", title: "채용공고별 맞춤 수정", desc: "공고 키워드에 맞춰 변형본을 만들어요", icon: <Edit />,        tone: 3 },
];

type Props = {
  target: Target | undefined;
  careerYear: number | undefined;
  targetJobs: Set<TargetJob>;
};

function formatCareer(careerYear: number | undefined): string {
  if (careerYear === undefined) return "—";
  if (careerYear === 0) return "신입 0년";
  if (careerYear >= 7) return "7+ 년차 이상";
  return `${careerYear} 년차`;
}

export function StepRecap({ target, careerYear, targetJobs }: Props) {
  const purposeLabel = target ? TARGET_META[target].title : "—";
  const careerLabel = formatCareer(careerYear);
  const positionLabel = targetJobs.size > 0
    ? [...targetJobs].map((j) => TARGET_JOB_META[j].name).join(" · ")
    : "—";

  return (
    <div className="onb-scene entering">
      <div className="eyebrow">STEP 4 / 4</div>
      <h2>내 경험을 한 번 정리하면,</h2>
      <p className="sub">모든 취업 준비가 조금 더 가벼워져요. 알려주신 정보로 이런 흐름이 자동으로 연결돼요.</p>

      <div className="recap">
        <div className="recap-row"><span className="recap-k">준비</span><b>{purposeLabel}</b></div>
        <div className="recap-divider" />
        <div className="recap-row"><span className="recap-k">경력</span><b>{careerLabel}</b></div>
        <div className="recap-divider" />
        <div className="recap-row"><span className="recap-k">직무</span><b>{positionLabel}</b></div>
      </div>

      <div className="sample-grid">
        {SAMPLES.map((s, i) => (
          <div key={s.kbd} className={`sample-card tone-${s.tone}`}>
            <div className="kbd mono">{s.kbd}</div>
            <div className="sample-ico">{s.icon}</div>
            <div className="sample-title">{s.title}</div>
            <div className="sample-desc">{s.desc}</div>
            {i < SAMPLES.length - 1 && <div className="sample-arrow" aria-hidden>→</div>}
          </div>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 타입체크 + 빌드**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npx tsc --noEmit 2>&1 | tail -5
npm run build 2>&1 | tail -8
```

Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/onboarding/step-recap.tsx
git commit -m "feat(onboarding): Step4 StepRecap 시안 재작성 (recap + sample-grid)

- recap: 준비/경력/직무 3 row (TARGET_META, careerYear, TARGET_JOB_META 매핑)
- sample-grid: PRAR/자소서/공고별 정적 3 카드 (NotebookPen/FileText/Edit)
- careerYear 라벨링: 0→\"신입 0년\", 1~6→\"N 년차\", 7+→\"7+ 년차 이상\""
```

---

## Task 6: WelcomeModal

**목표:** 제출 성공 후 표시될 modal.

**Files:**
- Create: `frontend/src/components/onboarding/welcome-modal.tsx`

- [ ] **Step 1: welcome-modal.tsx 작성**

`frontend/src/components/onboarding/welcome-modal.tsx`:

```tsx
"use client";

import { Target, TARGET_META } from "@/lib/enums/target";
import { TargetJob, TARGET_JOB_META } from "@/lib/enums/target-job";

type Props = {
  target: Target;
  careerYear: number;
  targetJobs: Set<TargetJob>;
  onDismiss: () => void;
};

function formatCareer(careerYear: number): string {
  if (careerYear === 0) return "신입 0년";
  if (careerYear >= 7) return "7+ 년차 이상";
  return `${careerYear} 년차`;
}

export function WelcomeModal({ target, careerYear, targetJobs, onDismiss }: Props) {
  const purposeLabel = TARGET_META[target].title;
  const careerLabel = formatCareer(careerYear);
  const positionLabel = targetJobs.size > 0
    ? [...targetJobs].map((j) => TARGET_JOB_META[j].name).join(" · ")
    : "—";

  return (
    <div className="onb-welcome" onClick={onDismiss}>
      <div className="card" onClick={(e) => e.stopPropagation()}>
        <span className="mascot-cloud lg">
          <span className="blush" />
        </span>
        <h3>준비 다 됐어요!</h3>
        <p>
          이제 흐름에 맞춰<br />
          자소서·기업분석·지원 일정을 함께 정리해드릴게요.
        </p>
        <div className="summary">
          <div className="row"><b>준비</b><span>{purposeLabel}</span></div>
          <div className="row"><b>경력</b><span>{careerLabel}</span></div>
          <div className="row"><b>직무</b><span>{positionLabel}</span></div>
        </div>
        <button type="button" className="btn primary lg" style={{ width: "100%" }} onClick={onDismiss}>
          대시보드로 이동
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 타입체크**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npx tsc --noEmit 2>&1 | tail -5
```

Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/onboarding/welcome-modal.tsx
git commit -m "feat(onboarding): WelcomeModal — 제출 성공 후 .onb-welcome modal

- 백드롭 클릭 + 대시보드 버튼 모두 onDismiss 호출
- 요약(준비/경력/직무) + 마스코트
- Escape 키 처리는 v1 deferred (5.0 ProfileMenu 와 동일선상)"
```

---

## Task 7: OnboardingFlow 재작성 + page.tsx 통합

**목표:** 시안 App() 의 2-col 구조로 OnboardingFlow 재작성. 5.2 의 데이터 로직 (useState/submit/postOnboarding) 유지. WelcomeModal 이 submit 성공 시 router.push("/") 인터셉트.

**Files:**
- Modify: `frontend/src/components/onboarding/onboarding-flow.tsx`

- [ ] **Step 1: onboarding-flow.tsx 재작성**

`frontend/src/components/onboarding/onboarding-flow.tsx` 전체 교체:

```tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { useAuth } from "@/contexts/auth-context";
import { postOnboarding, OnboardingApiError } from "@/lib/api/onboarding";
import { Target } from "@/lib/enums/target";
import { TargetJob } from "@/lib/enums/target-job";
import { BrandPanel } from "./brand-panel";
import { Stepper } from "./stepper";
import { StepPurpose } from "./step-purpose";
import { StepCareer } from "./step-career";
import { StepPositions } from "./step-positions";
import { StepRecap } from "./step-recap";
import { WelcomeModal } from "./welcome-modal";
import { Arrow } from "./icons";

export function OnboardingFlow() {
  const router = useRouter();
  const { refreshUser } = useAuth();

  const [current, setCurrent] = useState<1 | 2 | 3 | 4>(1);
  const [target, setTarget] = useState<Target | undefined>();
  const [careerYear, setCareerYear] = useState<number | undefined>();
  const [targetJobs, setTargetJobs] = useState<Set<TargetJob>>(new Set());
  const [submitting, setSubmitting] = useState(false);
  const [topError, setTopError] = useState<string | undefined>();
  const [showWelcome, setShowWelcome] = useState(false);

  function toggleJob(j: TargetJob) {
    setTargetJobs((prev) => {
      const next = new Set(prev);
      if (next.has(j)) next.delete(j);
      else next.add(j);
      return next;
    });
  }

  const canNext =
    (current === 1 && !!target) ||
    (current === 2 && careerYear !== undefined) ||
    (current === 3 && targetJobs.size > 0) ||
    current === 4;

  async function submit() {
    setSubmitting(true);
    setTopError(undefined);
    try {
      await postOnboarding({
        target: target!,
        careerYear: careerYear!,
        targetJobs: [...targetJobs],
      });
      await refreshUser();
      setShowWelcome(true);
    } catch (err) {
      if (err instanceof OnboardingApiError) {
        setTopError(err.body.message ?? "온보딩 저장에 실패했습니다.");
      } else {
        setTopError("잠시 후 다시 시도해주세요.");
      }
    } finally {
      setSubmitting(false);
    }
  }

  function dismissWelcome() {
    setShowWelcome(false);
    router.push("/");
  }

  function handlePrev() {
    setCurrent((c) => (c > 1 ? ((c - 1) as 1 | 2 | 3 | 4) : c));
  }

  function handleSkip() {
    router.push("/");
  }

  function handleNext() {
    if (current < 4) {
      setCurrent((c) => (c + 1) as 1 | 2 | 3 | 4);
    } else {
      submit();
    }
  }

  return (
    <div className="onb-shell">
      <BrandPanel />

      <section className="onb-right">
        <Stepper current={current} />

        {topError && (
          <div
            role="alert"
            style={{
              padding: "var(--space-3) var(--space-4)",
              background: "var(--color-semantic-error-bg)",
              color: "var(--color-semantic-error)",
              borderRadius: "var(--radius-md)",
              fontSize: "var(--fs-body-sm)",
              marginTop: "var(--space-4)",
            }}
          >
            {topError}
          </div>
        )}

        {current === 1 && <StepPurpose value={target} onChange={setTarget} />}
        {current === 2 && <StepCareer value={careerYear} onChange={setCareerYear} />}
        {current === 3 && <StepPositions value={targetJobs} onToggle={toggleJob} />}
        {current === 4 && <StepRecap target={target} careerYear={careerYear} targetJobs={targetJobs} />}

        <div className="onb-footer">
          <button
            type="button"
            className="btn ghost"
            disabled={current === 1 || submitting}
            onClick={handlePrev}
          >
            <Arrow dir="left" /> 이전
          </button>
          <div className="onb-footer-right">
            <button
              type="button"
              className="btn ghost skip"
              disabled={submitting}
              onClick={handleSkip}
            >
              나중에 할게요
            </button>
            <button
              type="button"
              className="btn primary"
              disabled={!canNext || submitting}
              onClick={handleNext}
            >
              {current === 4
                ? submitting ? "처리중…" : "시작하기"
                : "다음"}
              <Arrow dir="right" />
            </button>
          </div>
        </div>
      </section>

      {showWelcome && target !== undefined && careerYear !== undefined && (
        <WelcomeModal
          target={target}
          careerYear={careerYear}
          targetJobs={targetJobs}
          onDismiss={dismissWelcome}
        />
      )}
    </div>
  );
}
```

**핵심 변경 요약**:
- 시안의 `<div className="onb-stage">` 는 `(public)/onboarding/page.tsx` 의 `<main className="onb-stage">` 가 담당 → OnboardingFlow 는 그 안의 `.onb-shell` (2-col grid wrapper) 부터.
- BrandPanel 이 좌측, `.onb-right` section 이 우측.
- 마지막 step (current === 4) 의 "시작하기" 버튼은 `submit()` 직접 호출. 시안 그대로 — `canNext` 가 항상 true (Step4 엔 입력 없음).
- `<Button>` 컴포넌트 (5.2 의 ui/button) 사용 안 함 — 시안의 `.btn.primary/.btn.ghost` 클래스 직접 사용 (onboarding.css 가 정의함).
- 에러 메시지는 인라인 style 유지 (시안에 명시적 alert 스타일 없음, 기능 유지 우선).
- 새 상태: `showWelcome` boolean. 제출 성공 → `setShowWelcome(true)`. WelcomeModal 의 `onDismiss` 가 setShowWelcome(false) + router.push.

- [ ] **Step 2: 빌드 + curl 라우트 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm run build 2>&1 | tail -12
```

Expected: BUILD SUCCESSFUL.

- [ ] **Step 3: dev server 점검 (자동 부분만)**

```bash
cd /Users/sungjiwon/claude/2chi_v1/backend
./gradlew bootRun > /tmp/be.log 2>&1 &
BE=$!
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm run dev > /tmp/fe.log 2>&1 &
FE=$!

sleep 20

for p in / /me /onboarding /signup /login /terms /privacy; do
  curl -sf -o /dev/null "http://localhost:3000$p" && echo "OK $p" || echo "FAIL $p"
done

kill $BE $FE 2>/dev/null
wait 2>/dev/null
```

Expected: 모든 경로 OK. 시각 점검은 컨트롤러(human)가 별도 수행.

- [ ] **Step 4: 회귀 확인 (테스트는 다음 Task 8 에서 selector 조정 예정)**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm test 2>&1 | tail -15
```

Expected: 5.2 onboarding-flow 의 4번째 케이스 ("4단계 완료 + [완료] → fetch 호출 + push('/')") 가 실패할 가능성 — 라벨이 "완료" → "시작하기" 변경됨 + 성공 후 즉시 push 아닌 WelcomeModal 표시. 다른 케이스도 selector 일부 영향 가능. **본 Task 에선 어떤 케이스가 실패하는지 보고만**, 픽스는 Task 8.

- [ ] **Step 5: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/onboarding/onboarding-flow.tsx
git commit -m "feat(onboarding): OnboardingFlow 시안 2-col 구조로 재작성 + WelcomeModal 게이트

- BrandPanel + Stepper + 4 step + .onb-footer 의 2-col .onb-shell 구조
- 시안 .btn.primary/.btn.ghost 직접 사용 (ui/button 의존 제거)
- 라벨: \"완료\" → \"시작하기\", \"나중에\" → \"나중에 할게요\"
- 제출 성공 → setShowWelcome(true) → WelcomeModal → onDismiss = router.push('/')
- 5.2 데이터 로직 (useState + submit + postOnboarding + refreshUser + 에러) 무변경"
```

---

## Task 8: 테스트 selector 조정 + WelcomeModal step 추가

**목표:** 5.2 의 5케이스가 새 UI 구조에서 다시 PASS. WelcomeModal 등장 → 클릭 → push 단계 추가.

**Files:**
- Modify: `frontend/src/components/onboarding/__tests__/onboarding-flow.test.tsx`

- [ ] **Step 1: 현재 테스트 실패 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npx vitest run src/components/onboarding/__tests__/onboarding-flow.test.tsx 2>&1 | tail -30
```

실패 케이스 정확히 식별. 가장 흔한 실패 원인:
- `name: /^완료/` → 새 UI 는 "시작하기" 라벨. **변경 필요**.
- 성공 path 의 `expect(pushMock).toHaveBeenCalledWith("/")` → 새 UI 는 즉시 push 안 함 (WelcomeModal). **WelcomeModal 클릭 단계 추가 필요**.
- `name: /나중에/` → 새 UI 는 "나중에 할게요". 정규식은 PASS 할 듯 (부분 매치).

- [ ] **Step 2: onboarding-flow.test.tsx 수정**

`frontend/src/components/onboarding/__tests__/onboarding-flow.test.tsx` 의 두 케이스 수정:

**기존 4번째 케이스 (4단계 완료 → fetch 호출 + push('/'))** 를 다음으로 교체:

```tsx
  test("4단계 완료 + [시작하기] → fetch 호출 + WelcomeModal 표시 → [대시보드로 이동] 클릭 → push('/')", async () => {
    fetchMock.mockResolvedValueOnce(new Response("{}", { status: 200 }));
    refreshUserMock.mockResolvedValueOnce(undefined);

    const user = userEvent.setup();
    render(<OnboardingFlow />);

    await user.click(screen.getByRole("button", { name: /이직 준비 중/ }));
    await clickNext(user);

    await user.click(screen.getByRole("button", { name: "3 년차" }));
    await clickNext(user);

    await user.click(screen.getByRole("button", { name: /Backend/ }));
    await user.click(screen.getByRole("button", { name: /Infra \/ Cloud/ }));
    await clickNext(user);

    // 시작하기 클릭 → submit
    await user.click(screen.getByRole("button", { name: /^시작하기/ }));

    expect(fetchMock).toHaveBeenCalledTimes(1);
    const [, init] = fetchMock.mock.calls[0];
    const body = JSON.parse((init as RequestInit).body as string);
    expect(body.target).toBe("JOB_CHANGE");
    expect(body.careerYear).toBe(3);
    expect(body.targetJobs).toEqual(expect.arrayContaining(["BACKEND", "INFRA_CLOUD"]));

    expect(refreshUserMock).toHaveBeenCalled();

    // 이 시점에 push 아직 호출 안 됨 — WelcomeModal 표시
    expect(pushMock).not.toHaveBeenCalled();

    // WelcomeModal "대시보드로 이동" 클릭
    const dashboardBtn = await screen.findByRole("button", { name: /대시보드로 이동/ });
    await user.click(dashboardBtn);

    expect(pushMock).toHaveBeenCalledWith("/");
  });
```

**기존 5번째 케이스 ([나중에] 클릭 → fetch 미호출 + push('/'))**: selector 만 미세 조정 (`/나중에/` 정규식 유지하되 동작 확인). 만약 정규식이 "나중에 할게요" 도 매치한다면 변경 불필요. **현재 형태로 두고 Step 3 에서 검증**.

**기존 4번째 케이스 (API 400 응답 → 상단 알림)** 의 "완료" 라벨도 변경:

```tsx
    await user.click(screen.getByRole("button", { name: /^시작하기/ }));
```

- [ ] **Step 3: 테스트 실행 → 5 PASS 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npx vitest run src/components/onboarding/__tests__/onboarding-flow.test.tsx 2>&1 | tail -15
```

Expected: 5 케이스 모두 PASS.

만약 케이스 5 ([나중에] 클릭) 의 `name: /나중에/` 가 매치 안 되거나 잘못 매치되면, `name: /나중에 할게요/` 로 명시.

- [ ] **Step 4: 전체 회귀**

```bash
npm test 2>&1 | tail -10
```

Expected: 37 PASS.

- [ ] **Step 5: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/onboarding/__tests__/onboarding-flow.test.tsx
git commit -m "test(onboarding): 5.2 테스트 selector 조정 — '시작하기' 라벨 + WelcomeModal step

- 4단계 완료 케이스: '완료' → '시작하기' + WelcomeModal 등장 검증 + '대시보드로 이동' 클릭 후 push
- 5케이스 모두 PASS 유지 (신규 케이스 추가 없음)"
```

---

## Task 9: 종단 점검 + 푸시 + PR

- [ ] **Step 1: 전체 회귀**

```bash
cd /Users/sungjiwon/claude/2chi_v1/backend
./gradlew test 2>&1 | tail -3
cd ../frontend
rm -rf .next/dev
npm test 2>&1 | tail -10
npm run build 2>&1 | tail -10
```

Expected:
- 백엔드: 변경 없음, PASS
- 프론트: 37 PASS (변동 없음 — onboarding-flow.test.tsx 의 5케이스 selector 만 조정)
- BUILD SUCCESSFUL

- [ ] **Step 2: dev server e2e 점검**

```bash
cd /Users/sungjiwon/claude/2chi_v1/backend
./gradlew bootRun > /tmp/be.log 2>&1 &
BE=$!
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm run dev > /tmp/fe.log 2>&1 &
FE=$!

sleep 20

# 라우트 살아있음 확인
for p in / /me /applications /cover-letter /jobs /signup /login /onboarding /terms /privacy; do
  curl -sf -o /dev/null "http://localhost:3000$p" && echo "OK $p" || echo "FAIL $p"
done

kill $BE $FE 2>/dev/null
wait 2>/dev/null
```

Expected: 모든 경로 OK.

브라우저에서 직접 (수동 — 컨트롤러 수행):
- 신규 가입 → `/onboarding` 도달 → 좌측 BrandPanel + 우측 Stepper + Step1 카드 2개
- Step 1~3 진행 → 카드 시안 정합 (icon-wrap/tone/check 배지)
- Step 4 → recap + sample-grid 3 카드
- "시작하기" → WelcomeModal 표시 (마스코트 + 요약 3 row + "대시보드로 이동")
- "대시보드로 이동" 클릭 → `/` 도달

- [ ] **Step 3: 푸시**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git push -u origin fix/5.2-onboarding-design
```

- [ ] **Step 4: CI 대기**

```bash
sleep 30
gh run list --branch fix/5.2-onboarding-design --limit 1
gh run watch --exit-status
```

Expected: backend + frontend ✅.

- [ ] **Step 5: PR 생성**

```bash
gh pr create --base develop --head fix/5.2-onboarding-design \
  --title "fix(onboarding): 5.2 온보딩 UI 시안 정합성" \
  --body "$(cat <<'EOF'
## 요약
- 5.2 의 `/onboarding` 페이지 UI 를 시안 `design_system/pages/onboarding.{html,css,jsx}` 와 일치
- BE 변경 없음 — Target enum 2개, TargetJob enum 5개, careerYear number 그대로
- 데이터 매핑: 시안 PURPOSES 4개 → 2개만 (취업/이직), POSITIONS 8개 → 5개만 (FE/BE/Infra-Cloud/Infra-Ops/UI-UX)
- 시안 부가 요소 전부 포함 — 좌측 BrandPanel, Step 4 recap+sample-grid, Welcome modal, 감성 요소 (tape/mascot-cloud/blush/accent)
- 5.3 패턴 — 시안 onboarding.css 페이지 단위 import + 클래스 JSX

## 변경 파일
- 신규: `(public)/onboarding/onboarding.css` (~870줄, 시안 935줄에서 변환)
- 신규 컴포넌트: `brand-panel.tsx`, `welcome-modal.tsx`, `icons.tsx`
- 재작성: `onboarding-flow.tsx`, `stepper.tsx`, `step-purpose.tsx`, `step-career.tsx`, `step-positions.tsx`, `step-recap.tsx`
- 테스트: `onboarding-flow.test.tsx` 5케이스 selector 조정 (4번째 케이스에 WelcomeModal step 추가)

## 동작 변경
- 마지막 step 라벨: \"완료\" → \"시작하기\"
- skip 라벨: \"나중에\" → \"나중에 할게요\"
- 제출 성공 흐름: 즉시 `/` 이동 → **WelcomeModal 등장** → \"대시보드로 이동\" 클릭 → `/`

## 제외 (다음 브랜치)
- Target enum 4개 확장 (portfolio/explore) — BE + DB 마이그레이션 필요, 별도 작업
- TargetJob enum 8개 확장 (Publisher/Data·AI/기타) — 동일
- INFRA_OPS 의미 충돌 (VMware·NAS·백신 vs DevOps) — product 결정 필요
- WelcomeModal Escape 키 — 5.0 ProfileMenu 와 동일 deferred
- 반응형 모바일 — 시안 미디어쿼리 그대로만, v1.5+

## 검증
- 백엔드: 변경 없음, CI ✅
- 프론트: 37 Vitest PASS (변동 없음), build ✅
- 수동: `/onboarding` 시안 정합 (BrandPanel/Stepper/Step1~4/WelcomeModal)

## 시안 참조
- `design_system/pages/onboarding.html` / `onboarding.css` / `onboarding.jsx`
- 설계: [docs/superpowers/specs/2026-05-20-fix-5.2-onboarding-design.md](docs/superpowers/specs/2026-05-20-fix-5.2-onboarding-design.md)
- 플랜: [docs/superpowers/plans/2026-05-20-fix-5.2-onboarding-design.md](docs/superpowers/plans/2026-05-20-fix-5.2-onboarding-design.md)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

---

## 자체 점검

- [x] 모든 step 에 실제 코드/명령어 포함 (placeholder 없음)
- [x] 파일 경로는 절대경로 / 정확한 상대경로 (괄호 path 는 escape)
- [x] TDD 의식적 적용: Task 8 의 selector 조정은 "실행 → 실패 → 수정 → 재실행 → PASS" 흐름
- [x] 각 Task 끝에 commit — 작은 단위 빈번 커밋
- [x] Spec §1~§10 모든 결정 사항 → Task 1~9 에 매핑됨
  - §1 범위·제외 ✓ (Tasks 4/5/6/7 의 데이터 매핑 + WelcomeModal 만 추가, 시안 확장 항목은 모두 제외)
  - §2 데이터 매핑 ✓ Task 4 (Step1/2/3) + Task 5 (Step4 라벨링)
  - §3 파일 구조 ✓ 파일 맵과 동일
  - §4 컴포넌트별 매핑 ✓ Tasks 3/4/5/6
  - §5 동작 흐름 ✓ Task 7 의 OnboardingFlow + Task 8 의 테스트 조정
  - §6 CSS 통합 ✓ Task 1 의 변환 4건 (`body.onb-body→.onb-stage`, 중복 제거, ambient blob 주석, executable bit 정리)
  - §7 테스트 ✓ Task 8
  - §8 비기능 — 의도적 미커버 (런타임 변경 없음)
  - §9 변경 영향 — PR description 에 명시
  - §10 후속 트래킹 — PR description "제외" 섹션
