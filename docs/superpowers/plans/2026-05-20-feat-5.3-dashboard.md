# feat/5.3-dashboard Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 로그인 후 첫 화면(대시보드 홈)을 시안 `design_system/pages/dashboard.{html,css,jsx}` 기반으로 구현. /me 만 실데이터, 나머지 위젯은 Mock 상수.

**Architecture:** `(app)/page.tsx` 는 얇은 Server Component, 내부 `<DashboardContent />` 가 `"use client"` 로 `useAuth()` 데이터 + Mock 상수를 위젯에 주입. 시안 CSS 를 페이지 단위 import. 매칭 패널 자리는 TodayQuote(시안 Greeting aside 분리) 가 대체.

**Tech Stack:** Next.js (app router) / React / TypeScript / Vitest + @testing-library/react / 기존 AuthContext.

**관련 문서:**
- 설계: [docs/superpowers/specs/2026-05-20-feat-5.3-dashboard-design.md](../specs/2026-05-20-feat-5.3-dashboard-design.md)
- 시안: `design_system/pages/dashboard.html`, `dashboard.css`, `dashboard.jsx`
- 와이어프레임 §2: `design_system/uploads/2chi_v1_wireframes_v0.1.html`
- 프론트 주의: `frontend/AGENTS.md` — "Next.js you know" 경고. 의심나는 API는 `frontend/node_modules/next/dist/docs/` 먼저 확인 (이 플랜은 기존 사용 API만 사용)

---

## 파일 맵 (이 플랜에서 만들거나 수정할 파일 — 책임 1개씩)

| 파일 | 책임 | 생성/수정 |
|---|---|---|
| `frontend/src/app/(app)/page.tsx` | 대시보드 라우트 진입점 (Server) — dashboard.css import + `<section className="dash-page">` + `<DashboardContent />` | 수정 |
| `frontend/src/app/(app)/dashboard.css` | 시안 dashboard.css 의 변환 결과 (TopNav 영역 삭제, body→.dash-page) | 생성 |
| `frontend/src/lib/mock/dashboard.ts` | Mock 데이터 상수 + 타입 (`ScheduleItem`, `KpiCompletenessData`, …) | 생성 |
| `frontend/src/components/dashboard/icons.tsx` | dashboard 전용 인라인 SVG 12종 | 생성 |
| `frontend/src/components/dashboard/greeting.tsx` | 인사 헤더 (날짜·닉네임·부제 태그) | 생성 |
| `frontend/src/components/dashboard/kpi-completeness.tsx` | KPI: 작성 이력 완성도 | 생성 |
| `frontend/src/components/dashboard/kpi-cover-letters.tsx` | KPI: 자소서 작성 수 | 생성 |
| `frontend/src/components/dashboard/kpi-in-progress.tsx` | KPI: 진행중인 지원 | 생성 |
| `frontend/src/components/dashboard/upcoming-panel.tsx` | 다가오는 일정 표 | 생성 |
| `frontend/src/components/dashboard/today-quote.tsx` | "오늘의 한 줄" 마스코트 카피 (정적) | 생성 |
| `frontend/src/components/dashboard/shortcuts.tsx` | 바로가기 4개 (2 작동 + 2 disabled) | 생성 |
| `frontend/src/components/dashboard/dashboard-content.tsx` | 전체 조립 ("use client", useAuth + Mock 주입) | 생성 |
| `frontend/src/components/dashboard/__tests__/greeting.test.tsx` | Greeting 단위 테스트 (2 케이스) | 생성 |
| `frontend/src/components/dashboard/__tests__/dashboard-content.test.tsx` | 통합 렌더링 테스트 (5 케이스) | 생성 |

---

## Task 1: dashboard.css 복사·변환 + page.tsx 빈 셸

**목표:** dashboard.css 가 프론트에 로드되어 라우트가 빌드되는 상태. 시각 디테일은 아직 위젯이 없으니 빈 페이지.

**Files:**
- Create: `frontend/src/app/(app)/dashboard.css`
- Modify: `frontend/src/app/(app)/page.tsx`

- [ ] **Step 1: dashboard.css 복사**

```bash
cp /Users/sungjiwon/claude/2chi_v1/design_system/pages/dashboard.css /Users/sungjiwon/claude/2chi_v1/frontend/src/app/\(app\)/dashboard.css
```

- [ ] **Step 2: dashboard.css 변환 1 — TopNav 영역(L46~170) 삭제**

`frontend/src/app/(app)/dashboard.css` 열고 `/* === Top nav` 주석으로 시작하는 라인부터 (시안 L43 `/* ============================================================` 직전까지) `/* === Main layout` 주석 바로 위까지 통째로 삭제.

삭제 대상 헤더 영역:
```css
/* ============================================================
   Top nav
============================================================ */
.dash-nav { ... }
.dash-nav-inner { ... }
/* ... 그 외 .dash-nav-* / .nav-* / .nav-profile / .nav-icon 등 전체 블록 ... */
.nav-profile .caret { ... }
```

마지막 `.nav-profile .caret { ... }` (L166 부근) 직후, 다음 `/* === Main layout */` 주석 직전까지 전부 삭제. (정확히는 `.dash-nav` 로 시작하는 첫 줄부터 `Main layout` 헤더 직전까지)

확인 — 삭제 후 `grep -n "dash-nav\|nav-profile\|nav-icon" frontend/src/app/\(app\)/dashboard.css` 가 0건이어야 함:

```bash
grep -nE "dash-nav|nav-profile|nav-icon" /Users/sungjiwon/claude/2chi_v1/frontend/src/app/\(app\)/dashboard.css
```

Expected: (출력 없음)

- [ ] **Step 3: dashboard.css 변환 2 — `body.dash-body` → `.dash-page`**

상단 L7~41 블록을 다음과 같이 교체:

**Before:**
```css
body.dash-body {
  margin: 0;
  min-height: 100vh;
  background: var(--color-bg);
  font-family: var(--font-family-sans);
  color: var(--color-text-primary);
  position: relative;
  overflow-x: hidden;
}

/* Ambient soft wash — very restrained, only at the top behind the greeting */
body.dash-body::before {
  content: "";
  position: fixed;
  top: -340px; right: -240px;
  width: 820px; height: 820px;
  background:
    radial-gradient(closest-side, var(--color-primary-100), transparent 70%),
    radial-gradient(closest-side at 30% 70%, var(--color-lavender-100), transparent 75%);
  filter: blur(70px);
  opacity: 0.55;
  pointer-events: none;
  z-index: 0;
}
body.dash-body::after {
  content: "";
  position: fixed;
  top: 120px; left: -260px;
  width: 640px; height: 640px;
  background: radial-gradient(closest-side, var(--color-mint-100), transparent 70%);
  filter: blur(80px);
  opacity: 0.35;
  pointer-events: none;
  z-index: 0;
}
```

**After:**
```css
.dash-page {
  position: relative;
  overflow-x: hidden;
  background: var(--color-bg);
  color: var(--color-text-primary);
  min-height: calc(100vh - 64px); /* layout TopNav 높이 보정 */
}

/* Ambient soft wash — very restrained, only at the top behind the greeting */
.dash-page::before {
  content: "";
  position: fixed;
  top: -340px; right: -240px;
  width: 820px; height: 820px;
  background:
    radial-gradient(closest-side, var(--color-primary-100), transparent 70%),
    radial-gradient(closest-side at 30% 70%, var(--color-lavender-100), transparent 75%);
  filter: blur(70px);
  opacity: 0.55;
  pointer-events: none;
  z-index: 0;
}
.dash-page::after {
  content: "";
  position: fixed;
  top: 120px; left: -260px;
  width: 640px; height: 640px;
  background: radial-gradient(closest-side, var(--color-mint-100), transparent 70%);
  filter: blur(80px);
  opacity: 0.35;
  pointer-events: none;
  z-index: 0;
}
```

(`margin: 0`, `min-height: 100vh`, `font-family` 제거 — root layout 의 body 가 이미 담당. `min-height` 는 TopNav 64px 빼고 영역만큼.)

확인:
```bash
grep -nE "^body\.dash-body|dash-body" /Users/sungjiwon/claude/2chi_v1/frontend/src/app/\(app\)/dashboard.css
```

Expected: 출력 없음 (모든 `body.dash-body` 가 `.dash-page` 로 변환됨)

- [ ] **Step 4: page.tsx 임시 셸로 수정**

`frontend/src/app/(app)/page.tsx` 전체 교체:

```tsx
import "./dashboard.css";

export default function DashboardPage() {
  return (
    <section className="dash-page">
      <div className="dash-main">
        <p>Dashboard placeholder — Task 1</p>
      </div>
    </section>
  );
}
```

(기존 HomeBanner import 와 임시 콘텐츠는 Task 8 에서 DashboardContent 통해 다시 들어옴. 일시 제거 OK.)

- [ ] **Step 5: 빌드 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm run build 2>&1 | tail -15
```

Expected: `BUILD SUCCESSFUL` / Compiled successfully. dashboard.css 가 `/` 라우트에 bundle 됨.

- [ ] **Step 6: 기존 테스트 회귀 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm test 2>&1 | tail -8
```

Expected: 기존 30개 전부 PASS.

- [ ] **Step 7: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/app/\(app\)/dashboard.css frontend/src/app/\(app\)/page.tsx
git commit -m "feat(dashboard): dashboard.css 복사·변환 + 빈 셸 페이지

- 시안 dashboard.css 를 (app)/dashboard.css 로 복사
- TopNav 영역(.dash-nav*) 삭제 — 5.0 TopNav 와 충돌 방지
- body.dash-body → .dash-page 래퍼 셀렉터 (페이지 단위 스코프)
- page.tsx 가 dashboard.css import + 빈 .dash-page/.dash-main 셸 렌더"
```

---

## Task 2: Mock 데이터 + 타입 (lib/mock/dashboard.ts)

**목표:** 위젯이 import 할 Mock 상수와 타입을 정의. 시안 dashboard.jsx 의 값 그대로.

**Files:**
- Create: `frontend/src/lib/mock/dashboard.ts`

- [ ] **Step 1: lib/mock/dashboard.ts 작성**

`frontend/src/lib/mock/dashboard.ts`:

```ts
// ============================================================
// Dashboard Mock 데이터 (feat/5.3)
//
// 모든 값은 시안 design_system/pages/dashboard.jsx 와 동일.
// 추후 각 도메인 (5.4~5.8) 완성 시 위젯의 props 소스를 실 API 로 교체.
// ============================================================

export type GreetingTag = {
  label: string;
  tone?: "mint" | "lav" | "peach"; // .greet-tag.mint/.lav/.peach 매핑
};

export const GREETING_TAGS: GreetingTag[] = [
  { label: "백엔드" },
  { label: "중고신입 (2년차)", tone: "mint" },
  { label: "이직 준비 중", tone: "lav" },
];

export type KpiCompletenessPart = {
  name: string;
  pct: number;
  tone?: "mint" | "peach"; // .bar-row.mint/.peach 매핑
};

export type KpiCompletenessData = {
  total: number;
  parts: KpiCompletenessPart[];
};

export const KPI_COMPLETENESS_MOCK: KpiCompletenessData = {
  total: 72,
  parts: [
    { name: "이력서", pct: 90, tone: "mint" },
    { name: "경력 기술", pct: 70 },
    { name: "포트폴리오", pct: 55, tone: "peach" },
  ],
};

export type KpiCoverLettersMini = {
  k: string;
  v: number;
  unit: string;
};

export type KpiCoverLettersData = {
  total: number;
  totalUnit: string;
  mini: KpiCoverLettersMini[];
};

export const KPI_COVER_LETTERS_MOCK: KpiCoverLettersData = {
  total: 14,
  totalUnit: "건",
  mini: [
    { k: "이번 달", v: 5, unit: "건" },
    { k: "마스터", v: 6, unit: "개" },
    { k: "변형본", v: 14, unit: "개" },
  ],
};

export type KpiInProgressStage = {
  label: string;
  n: number;
  cls: "doc" | "code" | "int1" | "int2" | "exec";
};

export type KpiInProgressData = {
  total: number;
  totalUnit: string;
  stages: KpiInProgressStage[];
};

export const KPI_IN_PROGRESS_MOCK: KpiInProgressData = {
  total: 7,
  totalUnit: "건",
  stages: [
    { label: "서류", n: 3, cls: "doc" },
    { label: "코테", n: 1, cls: "code" },
    { label: "1차면접", n: 2, cls: "int1" },
    { label: "2차면접", n: 1, cls: "int2" },
  ],
};

export type ScheduleItem = {
  stage: string;       // "1차면접" | "코딩테스트" | "서류마감" | "임원면접"
  stageCls: "int1" | "int2" | "code" | "doc" | "exec";
  company: string;
  role: string;
  month: string;       // "MAY"
  day: string;         // "10"
  weekday: string;     // "MON"
  time: string;        // "14:00" | "23:59"
  dday: string;        // "D-2" | "오늘"
  soon?: boolean;
};

export const UPCOMING_MOCK: ScheduleItem[] = [
  {
    stage: "1차면접",
    stageCls: "int1",
    company: "(주)테크컴퍼니",
    role: "백엔드 (경력 2~5년)",
    month: "MAY",
    day: "10",
    weekday: "MON",
    time: "14:00",
    dday: "D-2",
    soon: true,
  },
  {
    stage: "코딩테스트",
    stageCls: "code",
    company: "네이버",
    role: "백엔드 신입 / 주니어",
    month: "MAY",
    day: "11",
    weekday: "TUE",
    time: "10:00",
    dday: "D-3",
  },
  {
    stage: "서류마감",
    stageCls: "doc",
    company: "카카오",
    role: "백엔드 (Saas 팀)",
    month: "MAY",
    day: "12",
    weekday: "WED",
    time: "23:59",
    dday: "오늘",
  },
  {
    stage: "임원면접",
    stageCls: "exec",
    company: "토스",
    role: "Server Engineer",
    month: "MAY",
    day: "14",
    weekday: "FRI",
    time: "16:00",
    dday: "D-6",
  },
];
```

- [ ] **Step 2: 타입체크**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npx tsc --noEmit 2>&1 | tail -5
```

Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/lib/mock/dashboard.ts
git commit -m "feat(dashboard): Mock 데이터 + 타입 (lib/mock/dashboard.ts)

- GREETING_TAGS / KPI_COMPLETENESS / KPI_COVER_LETTERS / KPI_IN_PROGRESS / UPCOMING
- 값은 시안 design_system/pages/dashboard.jsx 와 동일 (72/14/7, 일정 4건)"
```

---

## Task 3: dashboard 전용 icons.tsx

**목표:** 시안 dashboard.jsx 의 인라인 SVG 12종을 named export 로 포팅.

**Files:**
- Create: `frontend/src/components/dashboard/icons.tsx`

- [ ] **Step 1: icons.tsx 작성**

`frontend/src/components/dashboard/icons.tsx`:

```tsx
type IconProps = { size?: number; className?: string };

export function Bell({ size = 18, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <path d="M6 8a6 6 0 1 1 12 0c0 7 3 9 3 9H3s3-2 3-9" />
      <path d="M10.3 21a1.94 1.94 0 0 0 3.4 0" />
    </svg>
  );
}

export function ChevronDown({ size = 14, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <polyline points="6 9 12 15 18 9" />
    </svg>
  );
}

export function ArrowRight({ size = 14, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <line x1="5" y1="12" x2="19" y2="12" />
      <polyline points="13 6 19 12 13 18" />
    </svg>
  );
}

export function FileEdit({ size = 18, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
      <polyline points="14 2 14 8 20 8" />
      <path d="M10.5 17.5l-2 .5.5-2 5-5 1.5 1.5z" />
    </svg>
  );
}

export function Layers({ size = 18, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <path d="m12 2 9 5-9 5-9-5z" />
      <path d="m3 12 9 5 9-5" />
      <path d="m3 17 9 5 9-5" />
    </svg>
  );
}

export function Briefcase({ size = 18, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <rect x="2" y="6" width="20" height="14" rx="2" />
      <path d="M16 6V4a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2" />
      <path d="M2 13h20" />
    </svg>
  );
}

export function Calendar({ size = 18, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <rect x="3" y="4" width="18" height="18" rx="2" />
      <line x1="16" y1="2" x2="16" y2="6" />
      <line x1="8" y1="2" x2="8" y2="6" />
      <line x1="3" y1="10" x2="21" y2="10" />
    </svg>
  );
}

export function Target({ size = 18, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <circle cx="12" cy="12" r="9" />
      <circle cx="12" cy="12" r="5" />
      <circle cx="12" cy="12" r="1" />
    </svg>
  );
}

export function Plus({ size = 16, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <line x1="12" y1="5" x2="12" y2="19" />
      <line x1="5" y1="12" x2="19" y2="12" />
    </svg>
  );
}

export function Building({ size = 18, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <rect x="4" y="3" width="16" height="18" rx="1" />
      <line x1="9" y1="8" x2="11" y2="8" />
      <line x1="13" y1="8" x2="15" y2="8" />
      <line x1="9" y1="12" x2="11" y2="12" />
      <line x1="13" y1="12" x2="15" y2="12" />
      <line x1="9" y1="16" x2="11" y2="16" />
      <line x1="13" y1="16" x2="15" y2="16" />
    </svg>
  );
}

export function Sparkle({ size = 14, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.6" strokeLinejoin="round" className={className}>
      <path d="M12 3l1.6 4.6L18 9l-4.4 1.4L12 15l-1.6-4.6L6 9l4.4-1.4z" />
    </svg>
  );
}

export function Search({ size = 16, className }: IconProps) {
  return (
    <svg viewBox="0 0 24 24" width={size} height={size} fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" className={className}>
      <circle cx="11" cy="11" r="7" />
      <line x1="21" y1="21" x2="16.65" y2="16.65" />
    </svg>
  );
}
```

- [ ] **Step 2: 타입체크**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npx tsc --noEmit 2>&1 | tail -5
```

Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/dashboard/icons.tsx
git commit -m "feat(dashboard): 인라인 SVG 아이콘 12종 (icons.tsx)

시안 dashboard.jsx 의 Ico.* 그대로 포팅 (Bell/ChevronDown/ArrowRight/FileEdit/Layers/
Briefcase/Calendar/Target/Plus/Building/Sparkle/Search). size·className props 통일."
```

---

## Task 4: Greeting 컴포넌트 + 테스트 (TDD)

**목표:** 인사 헤더 — 닉네임·날짜·정적 카피·부제 태그 (showTags 분기). 날짜 포맷이 유일한 실제 로직이라 TDD 로.

**Files:**
- Create: `frontend/src/components/dashboard/__tests__/greeting.test.tsx`
- Create: `frontend/src/components/dashboard/greeting.tsx`

- [ ] **Step 1: greeting.test.tsx 작성 (failing)**

`frontend/src/components/dashboard/__tests__/greeting.test.tsx`:

```tsx
import { describe, expect, test, vi, beforeAll, afterAll } from "vitest";
import { render, screen } from "@testing-library/react";
import { Greeting } from "../greeting";

describe("Greeting", () => {
  beforeAll(() => {
    vi.useFakeTimers();
    vi.setSystemTime(new Date(2026, 4, 20, 10, 0, 0)); // 2026-05-20 (수요일) 10:00
  });

  afterAll(() => {
    vi.useRealTimers();
  });

  test("닉네임 prop 이 h1 에 표시된다", () => {
    render(<Greeting nickname="김소미" showTags={true} />);
    expect(screen.getByRole("heading", { level: 1 }).textContent).toMatch(/안녕하세요, 김소미님/);
  });

  test("날짜를 'YYYY · MM · DD 요일 · 오늘의 준비 현황' 형식으로 표시한다", () => {
    render(<Greeting nickname="X" showTags={true} />);
    // 2026-05-20 은 수요일
    expect(screen.getByText(/2026 · 05 · 20 수요일 · 오늘의 준비 현황/)).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 테스트 실행 — fail 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npx vitest run src/components/dashboard/__tests__/greeting.test.tsx 2>&1 | tail -10
```

Expected: FAIL — "Cannot find module '../greeting'" 또는 import 에러.

- [ ] **Step 3: greeting.tsx 구현**

`frontend/src/components/dashboard/greeting.tsx`:

```tsx
"use client";

import { GREETING_TAGS } from "@/lib/mock/dashboard";

type Props = {
  nickname: string;
  showTags: boolean;
};

function formatGreetingDate(now: Date): string {
  const parts = new Intl.DateTimeFormat("ko-KR", {
    year: "numeric",
    month: "2-digit",
    day: "2-digit",
    weekday: "long",
  }).formatToParts(now);
  const year = parts.find((p) => p.type === "year")?.value ?? "";
  const month = parts.find((p) => p.type === "month")?.value ?? "";
  const day = parts.find((p) => p.type === "day")?.value ?? "";
  const weekday = parts.find((p) => p.type === "weekday")?.value ?? "";
  return `${year} · ${month} · ${day} ${weekday} · 오늘의 준비 현황`;
}

export function Greeting({ nickname, showTags }: Props) {
  const dateLabel = formatGreetingDate(new Date());

  return (
    <section className="greet">
      <div className="greet-text">
        <div className="greet-meta" suppressHydrationWarning>
          {dateLabel}
        </div>
        <h1>
          안녕하세요, {nickname}님
          <span className="wave" role="img" aria-label="hi">
            👋
          </span>
        </h1>
        <p className="line2">
          오늘도 이취가 다가오는 일정과 작성 흐름을 같이 정리해드릴게요. 내 이력과 지원 현황을 기준으로, 이번 주에 챙기면 좋을 준비를 모아뒀어요.
        </p>
        {showTags && (
          <div className="greet-tags">
            {GREETING_TAGS.map((t) => (
              <span key={t.label} className={`greet-tag${t.tone ? ` ${t.tone}` : ""}`}>
                <span className="swatch" />
                {t.label}
              </span>
            ))}
          </div>
        )}
      </div>
    </section>
  );
}
```

(시안의 `.greet-aside` 는 별도 TodayQuote 컴포넌트로 옮기므로 여기엔 없음.)

- [ ] **Step 4: 테스트 실행 — pass 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npx vitest run src/components/dashboard/__tests__/greeting.test.tsx 2>&1 | tail -10
```

Expected: 2 케이스 PASS.

만약 날짜 케이스가 실패하면 `formatToParts` 결과가 노드 ICU 버전에 따라 "월요일" 같은 형태일 수 있음 → vitest 실행 후 출력에서 실제 문자열 확인 후 테스트 정규식 조정. (예: `2026 · 05 · 20 수요일` 가 안 나오면 vitest 출력의 received 값을 보고 패턴 맞춤)

- [ ] **Step 5: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/dashboard/greeting.tsx frontend/src/components/dashboard/__tests__/greeting.test.tsx
git commit -m "feat(dashboard): Greeting 컴포넌트 + 테스트 2케이스 (TDD)

- nickname prop 표시
- 현재 날짜를 'YYYY · MM · DD 요일 · 오늘의 준비 현황' 으로 포매팅
  (Intl.DateTimeFormat.formatToParts 로 ' · ' 결합)
- showTags=false 일 때 부제 태그 숨김 (온보딩 미완료자)
- SSR/CSR 날짜 미스매치 회피로 suppressHydrationWarning 사용"
```

---

## Task 5: 3-card KPI 위젯

**목표:** 시안 `KpiCompleteness / KpiCoverLetters / KpiInProgress` 세 위젯 — 각각 props 받아 시안 시각 그대로 렌더.

**Files:**
- Create: `frontend/src/components/dashboard/kpi-completeness.tsx`
- Create: `frontend/src/components/dashboard/kpi-cover-letters.tsx`
- Create: `frontend/src/components/dashboard/kpi-in-progress.tsx`

- [ ] **Step 1: kpi-completeness.tsx 작성**

`frontend/src/components/dashboard/kpi-completeness.tsx`:

```tsx
import type { KpiCompletenessData } from "@/lib/mock/dashboard";
import { Layers } from "./icons";

type Props = { data: KpiCompletenessData };

export function KpiCompleteness({ data }: Props) {
  return (
    <article className="kpi tone-mint">
      <div className="kpi-head">
        <span className="lbl">내 작성 이력 완성도</span>
        <span className="ico">
          <Layers />
        </span>
      </div>
      <div className="kpi-value">
        <span className="num">{data.total}</span>
        <span className="unit">%</span>
      </div>
      <div className="kpi-foot">
        {data.parts.map((p) => (
          <div key={p.name} className={`bar-row${p.tone ? ` ${p.tone}` : ""}`}>
            <span className="nm">{p.name}</span>
            <span className="track">
              <span style={{ width: `${p.pct}%` }} />
            </span>
            <span className="pct">{p.pct}</span>
          </div>
        ))}
      </div>
    </article>
  );
}
```

- [ ] **Step 2: kpi-cover-letters.tsx 작성**

`frontend/src/components/dashboard/kpi-cover-letters.tsx`:

```tsx
import type { KpiCoverLettersData } from "@/lib/mock/dashboard";
import { FileEdit } from "./icons";

type Props = { data: KpiCoverLettersData };

export function KpiCoverLetters({ data }: Props) {
  return (
    <article className="kpi">
      <div className="kpi-head">
        <span className="lbl">자소서 작성 수</span>
        <span className="ico">
          <FileEdit />
        </span>
      </div>
      <div className="kpi-value">
        <span className="num">{data.total}</span>
        <span className="unit">{data.totalUnit}</span>
      </div>
      <div className="kpi-foot">
        <div className="mini-stats">
          {data.mini.map((m) => (
            <div key={m.k} className="mini">
              <span className="k">{m.k}</span>
              <span className="v">
                {m.v}
                <em>{m.unit}</em>
              </span>
            </div>
          ))}
        </div>
      </div>
    </article>
  );
}
```

- [ ] **Step 3: kpi-in-progress.tsx 작성**

`frontend/src/components/dashboard/kpi-in-progress.tsx`:

```tsx
import type { KpiInProgressData } from "@/lib/mock/dashboard";
import { Briefcase } from "./icons";

type Props = { data: KpiInProgressData };

export function KpiInProgress({ data }: Props) {
  return (
    <article className="kpi tone-lav">
      <span className="kpi-ring" aria-hidden="true" />
      <div className="kpi-head">
        <span className="lbl">진행 중인 지원</span>
        <span className="ico">
          <Briefcase />
        </span>
      </div>
      <div className="kpi-value">
        <span className="num">{data.total}</span>
        <span className="unit">{data.totalUnit}</span>
      </div>
      <div className="kpi-foot">
        <div className="stage-row">
          {data.stages.map((s) => (
            <span key={s.cls} className={`stage ${s.cls} dot`}>
              {s.label} <span className="n">{s.n}</span>
            </span>
          ))}
        </div>
      </div>
    </article>
  );
}
```

- [ ] **Step 4: 타입체크 + 빌드 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npx tsc --noEmit 2>&1 | tail -5
npm run build 2>&1 | tail -8
```

Expected: 에러 없음 / BUILD SUCCESSFUL.

- [ ] **Step 5: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/dashboard/kpi-completeness.tsx frontend/src/components/dashboard/kpi-cover-letters.tsx frontend/src/components/dashboard/kpi-in-progress.tsx
git commit -m "feat(dashboard): 3-card KPI 위젯 (완성도/자소서/진행중)

- KpiCompleteness: tone-mint, 이력서/경력/포폴 3-row 프로그레스
- KpiCoverLetters: 자소서 총수 + 이번달/마스터/변형본 mini-stats
- KpiInProgress: tone-lav, 서류/코테/1차/2차 stage 칩
- 데이터는 props 로 주입 (KpiCompletenessData / KpiCoverLettersData / KpiInProgressData)"
```

---

## Task 6: UpcomingPanel + TodayQuote

**목표:** dual-grid 좌(다가오는 일정) + 우(오늘의 한 줄).

**Files:**
- Create: `frontend/src/components/dashboard/upcoming-panel.tsx`
- Create: `frontend/src/components/dashboard/today-quote.tsx`

- [ ] **Step 1: upcoming-panel.tsx 작성**

`frontend/src/components/dashboard/upcoming-panel.tsx`:

```tsx
import type { ScheduleItem } from "@/lib/mock/dashboard";
import { Calendar, ArrowRight } from "./icons";

type Props = { items: ScheduleItem[] };

export function UpcomingPanel({ items }: Props) {
  return (
    <section className="panel">
      <div className="panel-head">
        <div className="title">
          <span className="ico">
            <Calendar size={16} />
          </span>
          다가오는 일정
        </div>
        <span className="more" aria-disabled="true" title="준비중">
          캘린더 보기 <ArrowRight />
        </span>
      </div>
      <div className="sched-list">
        {items.map((s, i) => (
          <div key={i} className={`sched-row${s.soon ? " soon" : ""}`}>
            <div className="sched-date">
              <span className="m">{s.month}</span>
              <span className="d">{s.day}</span>
              <span className="wd">{s.weekday}</span>
            </div>
            <div className="sched-info">
              <div className="co">{s.company}</div>
              <div className="meta">
                <span className={`stage ${s.stageCls}`}>{s.stage}</span>
                <span className="dot" />
                <span>{s.role}</span>
              </div>
            </div>
            <div className="sched-time">{s.time}</div>
            <div className="sched-dday">{s.dday}</div>
          </div>
        ))}
      </div>
    </section>
  );
}
```

(시안의 "캘린더 보기" 더보기 링크는 캘린더 라우트 미존재 → `<a>` 대신 `<span aria-disabled>` 로 비활성화.)

- [ ] **Step 2: today-quote.tsx 작성**

`frontend/src/components/dashboard/today-quote.tsx`:

```tsx
export function TodayQuote() {
  return (
    <aside className="greet-aside today-quote" aria-hidden="false">
      <span className="mascot-cloud" aria-hidden="true" />
      <div className="copy">
        <small>오늘의 한 줄</small>
        이번 주는 1차 면접 두 곳,
        <br />
        차근히 준비해봐요.
      </div>
    </aside>
  );
}
```

(시안 Greeting 의 `.greet-aside` 를 그대로 옮김. 매칭 패널 자리에 위치하므로 `dual-grid` 의 우측 자리에 들어감. dashboard.css 의 `.greet-aside` 스타일이 그대로 적용됨 — 일정 패널 옆에서 보이는 외관은 시안과 다를 수 있어 dev server 점검에서 시각 확인 필수.)

추가 검증: `.greet-aside` 가 시안에서는 `.greet` 안에 들어 있어서 부모 컨텍스트가 다름. dashboard.css 의 `.greet-aside` 가 absolute/grid 자식 의존이라면 dual-grid 안에서 깨질 수 있음. Step 4 의 dev server 점검에서 확인하고, 깨지면 dashboard.css 의 `.today-quote` 셀렉터로 override 추가 (Task 8 통합 후 수정).

- [ ] **Step 3: 타입체크 + 빌드 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npx tsc --noEmit 2>&1 | tail -5
npm run build 2>&1 | tail -8
```

Expected: 에러 없음.

- [ ] **Step 4: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/dashboard/upcoming-panel.tsx frontend/src/components/dashboard/today-quote.tsx
git commit -m "feat(dashboard): UpcomingPanel + TodayQuote

- UpcomingPanel: 다가오는 일정 표 (props 일정 배열)
  - 캘린더 보기 더보기는 라우트 미존재 → aria-disabled span
- TodayQuote: 시안 Greeting aside 분리 — 매칭 패널 자리에 배치"
```

---

## Task 7: Shortcuts (4개 + disabled 2개)

**목표:** 바로가기 4개 카드 — 앞 2개는 Next.js `<Link>`, 뒤 2개는 disabled 버튼 + "준비중" 배지.

**Files:**
- Create: `frontend/src/components/dashboard/shortcuts.tsx`

- [ ] **Step 1: shortcuts.tsx 작성**

`frontend/src/components/dashboard/shortcuts.tsx`:

```tsx
import Link from "next/link";
import { Sparkle, Plus, Building, Calendar } from "./icons";

type ShortcutItem = {
  label: string;
  sub: string;
  toneCls: string; // .shortcut.primary | .shortcut.tone-1 | .tone-2 | .tone-3
  icon: React.ReactNode;
  href?: string;   // 있으면 작동, 없으면 disabled
};

const ITEMS: ShortcutItem[] = [
  {
    label: "자소서 작성",
    sub: "AI 초안 → 수정본 흐름",
    toneCls: "primary",
    icon: <Sparkle size={16} />,
    href: "/cover-letter",
  },
  {
    label: "채용공고 등록",
    sub: "URL 붙여넣기 · 직접 작성",
    toneCls: "tone-1",
    icon: <Plus size={16} />,
    href: "/jobs",
  },
  {
    label: "기업분석 시작",
    sub: "컬처 · 도메인 · 기술 스택",
    toneCls: "tone-2",
    icon: <Building />,
  },
  {
    label: "캘린더 보기",
    sub: "월·주·일 전형 일정",
    toneCls: "tone-3",
    icon: <Calendar />,
  },
];

export function Shortcuts() {
  return (
    <section className="shortcuts">
      <div className="lead">
        <span className="k">SHORTCUTS</span>
        <span className="t">다음 한 걸음, 어디부터 할까요?</span>
      </div>
      <div className="shortcuts-grid">
        {ITEMS.map((it) =>
          it.href ? (
            <Link key={it.label} className={`shortcut ${it.toneCls}`} href={it.href}>
              <span className="ico">{it.icon}</span>
              <div className="body">
                <span className="nm">{it.label}</span>
                <span className="sub">{it.sub}</span>
              </div>
            </Link>
          ) : (
            <div
              key={it.label}
              className={`shortcut ${it.toneCls} is-disabled`}
              role="link"
              aria-disabled="true"
              tabIndex={-1}
              style={{ opacity: 0.55, cursor: "not-allowed", position: "relative" }}
            >
              <span className="ico">{it.icon}</span>
              <div className="body">
                <span className="nm">{it.label}</span>
                <span className="sub">{it.sub}</span>
              </div>
              <span
                style={{
                  position: "absolute",
                  top: 8,
                  right: 10,
                  fontSize: 10,
                  fontWeight: 600,
                  padding: "2px 6px",
                  borderRadius: 4,
                  background: "var(--color-text-muted)",
                  color: "var(--color-bg)",
                }}
              >
                준비중
              </span>
            </div>
          )
        )}
      </div>
    </section>
  );
}
```

(시안 `.shortcut.primary/.tone-1/.tone-2/.tone-3` 스타일은 dashboard.css 가 제공. disabled 처리는 inline style 로만 — dashboard.css 에 별도 `.is-disabled` 클래스 추가 없음.)

- [ ] **Step 2: 타입체크 + 빌드 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npx tsc --noEmit 2>&1 | tail -5
npm run build 2>&1 | tail -8
```

Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/dashboard/shortcuts.tsx
git commit -m "feat(dashboard): Shortcuts 4개 (2 작동 + 2 disabled)

- 자소서 작성 → /cover-letter (작동)
- 채용공고 등록 → /jobs (작동)
- 기업분석 시작 / 캘린더 보기 → aria-disabled + 준비중 배지
  (라우트 /companies, /calendar 는 5.10/5.8 도메인에서 생성)"
```

---

## Task 8: DashboardContent 조립 + page.tsx 통합

**목표:** 모든 위젯을 useAuth + Mock 상수와 함께 조립. page.tsx 는 dashboard.css import + `<DashboardContent />` 만.

**Files:**
- Create: `frontend/src/components/dashboard/dashboard-content.tsx`
- Modify: `frontend/src/app/(app)/page.tsx`

- [ ] **Step 1: dashboard-content.tsx 작성**

`frontend/src/components/dashboard/dashboard-content.tsx`:

```tsx
"use client";

import { useAuth } from "@/contexts/auth-context";
import { HomeBanner } from "@/components/home/home-banner";
import { Greeting } from "./greeting";
import { KpiCompleteness } from "./kpi-completeness";
import { KpiCoverLetters } from "./kpi-cover-letters";
import { KpiInProgress } from "./kpi-in-progress";
import { UpcomingPanel } from "./upcoming-panel";
import { TodayQuote } from "./today-quote";
import { Shortcuts } from "./shortcuts";
import {
  KPI_COMPLETENESS_MOCK,
  KPI_COVER_LETTERS_MOCK,
  KPI_IN_PROGRESS_MOCK,
  UPCOMING_MOCK,
} from "@/lib/mock/dashboard";

export function DashboardContent() {
  const { user } = useAuth();
  if (!user) return null;

  return (
    <div className="dash-main">
      <HomeBanner />
      <Greeting nickname={user.nickname} showTags={user.onboardingCompleted} />
      <div className="kpi-grid">
        <KpiCompleteness data={KPI_COMPLETENESS_MOCK} />
        <KpiCoverLetters data={KPI_COVER_LETTERS_MOCK} />
        <KpiInProgress data={KPI_IN_PROGRESS_MOCK} />
      </div>
      <div className="dual-grid">
        <UpcomingPanel items={UPCOMING_MOCK} />
        <TodayQuote />
      </div>
      <Shortcuts />
    </div>
  );
}
```

(layout 가드가 비로그인 시 `/login` 으로 보내므로 `user === null` 인 상태로 여기 도달할 일은 거의 없지만 안전 가드 + TypeScript narrowing 용도로 early return.)

- [ ] **Step 2: page.tsx 최종 업데이트**

`frontend/src/app/(app)/page.tsx` 전체 교체:

```tsx
import "./dashboard.css";
import { DashboardContent } from "@/components/dashboard/dashboard-content";

export default function DashboardPage() {
  return (
    <section className="dash-page">
      <DashboardContent />
    </section>
  );
}
```

- [ ] **Step 3: 빌드 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm run build 2>&1 | tail -10
```

Expected: BUILD SUCCESSFUL. `/` 라우트에 dashboard.css + 컴포넌트 트리 bundle.

- [ ] **Step 4: dev server 시각 점검 (수동)**

```bash
cd /Users/sungjiwon/claude/2chi_v1/backend
./gradlew bootRun > /tmp/be.log 2>&1 &
BE=$!
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm run dev > /tmp/fe.log 2>&1 &
FE=$!

sleep 20
```

브라우저에서 http://localhost:3000 로그인 → `/` 도달.

확인 사항:
- 인사 헤더에 닉네임 + 부제 태그 3개 (온보딩 완료자) / 또는 HomeBanner + 닉네임만 (미완료자)
- 3-card KPI (72% / 14건 / 7건) 시안과 동일
- 좌측 일정 4건 + 우측 "오늘의 한 줄" 카드
- 바로가기 4개 — 앞 2개 클릭 시 /cover-letter, /jobs 이동 / 뒤 2개 클릭 무반응 + 우상단 "준비중" 배지
- ::before/::after 그래디언트 워시가 헤더 뒤에 은은하게 보임

문제 발견 시:
- TodayQuote 가 dual-grid 안에서 깨지면 → dashboard.css 에 `.dual-grid > .today-quote { ... }` override 추가
- ::before/::after 위치 어색하면 → `position: fixed` 가 layout main 의 transform 컨텍스트에 영향받는지 확인, 필요 시 `.dash-page` 의 isolation: isolate 추가

수동 점검 종료:

```bash
kill $BE $FE 2>/dev/null
wait 2>/dev/null
```

- [ ] **Step 5: 기존 테스트 회귀 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm test 2>&1 | tail -10
```

Expected: 기존 30 + greeting 2 = 32 PASS.

- [ ] **Step 6: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/dashboard/dashboard-content.tsx frontend/src/app/\(app\)/page.tsx
git commit -m "feat(dashboard): DashboardContent 조립 + page.tsx 통합

- DashboardContent (\"use client\") 가 useAuth + Mock 상수를 위젯에 주입
- HomeBanner 는 .dash-main 안에 배치 (1280px 컨테이너 내)
- page.tsx 는 dashboard.css import + .dash-page 셸 + DashboardContent
- 비로그인 가드는 (app)/layout 이 담당, user null 안전 early return"
```

---

## Task 9: 통합 테스트 — dashboard-content.test.tsx (5 케이스, TDD)

**목표:** Greeting 의 showTags 분기 / HomeBanner 표시 분기 / 모든 위젯 렌더 / disabled shortcuts aria 검증.

**Files:**
- Create: `frontend/src/components/dashboard/__tests__/dashboard-content.test.tsx`

- [ ] **Step 1: dashboard-content.test.tsx 작성**

`frontend/src/components/dashboard/__tests__/dashboard-content.test.tsx`:

```tsx
import { describe, expect, test, vi, beforeEach } from "vitest";
import { render, screen, within } from "@testing-library/react";
import { DashboardContent } from "../dashboard-content";

type AuthUser = { userId: number; email: string; nickname: string; onboardingCompleted: boolean };
type AuthMock = {
  user: AuthUser | null;
  initialized: boolean;
  login: ReturnType<typeof vi.fn>;
  logout: ReturnType<typeof vi.fn>;
  refreshUser: ReturnType<typeof vi.fn>;
};

let authMock: AuthMock = {
  user: { userId: 1, email: "kim@example.com", nickname: "김소미", onboardingCompleted: true },
  initialized: true,
  login: vi.fn(),
  logout: vi.fn(),
  refreshUser: vi.fn(),
};

vi.mock("@/contexts/auth-context", () => ({
  useAuth: () => authMock,
}));

// HomeBanner 안의 next/link 처리
vi.mock("next/link", () => ({
  default: ({ href, children, ...rest }: { href: string; children: React.ReactNode } & React.AnchorHTMLAttributes<HTMLAnchorElement>) => (
    <a href={href} {...rest}>
      {children}
    </a>
  ),
}));

beforeEach(() => {
  vi.useFakeTimers();
  vi.setSystemTime(new Date(2026, 4, 20, 10, 0, 0));
  authMock = {
    user: { userId: 1, email: "kim@example.com", nickname: "김소미", onboardingCompleted: true },
    initialized: true,
    login: vi.fn(),
    logout: vi.fn(),
    refreshUser: vi.fn(),
  };
});

describe("DashboardContent", () => {
  test("온보딩 완료자: 닉네임 + 부제 태그 3개 + HomeBanner 미표시", () => {
    render(<DashboardContent />);
    // 닉네임
    expect(screen.getByRole("heading", { level: 1 }).textContent).toMatch(/김소미님/);
    // 부제 태그 3개
    expect(screen.getByText("백엔드")).toBeInTheDocument();
    expect(screen.getByText("중고신입 (2년차)")).toBeInTheDocument();
    expect(screen.getByText("이직 준비 중")).toBeInTheDocument();
    // HomeBanner (role="status") 미존재
    expect(screen.queryByRole("status")).not.toBeInTheDocument();
  });

  test("온보딩 미완료자: 닉네임 표시 + 부제 태그 숨김 + HomeBanner 표시", () => {
    authMock.user = { userId: 1, email: "kim@example.com", nickname: "김소미", onboardingCompleted: false };
    render(<DashboardContent />);
    expect(screen.getByRole("heading", { level: 1 }).textContent).toMatch(/김소미님/);
    expect(screen.queryByText("백엔드")).not.toBeInTheDocument();
    expect(screen.queryByText("이직 준비 중")).not.toBeInTheDocument();
    expect(screen.getByRole("status")).toBeInTheDocument();
  });

  test("3-card KPI 렌더 — 완성도 72 / 자소서 14 / 진행중 7", () => {
    render(<DashboardContent />);
    // 각 카드 article 로 scope 좁혀서 num 검증 (다른 위젯과 동일 숫자 충돌 회피)
    const completenessCard = screen.getByText("내 작성 이력 완성도").closest("article");
    expect(completenessCard).not.toBeNull();
    expect(within(completenessCard!).getByText("72")).toBeInTheDocument();

    const coverLettersCard = screen.getByText("자소서 작성 수").closest("article");
    expect(coverLettersCard).not.toBeNull();
    expect(within(coverLettersCard!).getByText("14")).toBeInTheDocument();

    const inProgressCard = screen.getByText("진행 중인 지원").closest("article");
    expect(inProgressCard).not.toBeNull();
    expect(within(inProgressCard!).getByText("7")).toBeInTheDocument();
  });

  test("UpcomingPanel — Mock 일정 4건 회사명·시간 노출", () => {
    render(<DashboardContent />);
    expect(screen.getByText("(주)테크컴퍼니")).toBeInTheDocument();
    expect(screen.getByText("네이버")).toBeInTheDocument();
    expect(screen.getByText("카카오")).toBeInTheDocument();
    expect(screen.getByText("토스")).toBeInTheDocument();
    expect(screen.getByText("14:00")).toBeInTheDocument();
    expect(screen.getByText("23:59")).toBeInTheDocument();
  });

  test("Shortcuts 4개 — 앞 2개 작동, 뒤 2개 aria-disabled", () => {
    render(<DashboardContent />);
    // 작동 — 자소서 작성 / 채용공고 등록
    const coverLetter = screen.getByText("자소서 작성").closest("a");
    expect(coverLetter).toHaveAttribute("href", "/cover-letter");
    const jobs = screen.getByText("채용공고 등록").closest("a");
    expect(jobs).toHaveAttribute("href", "/jobs");
    // disabled — 기업분석 / 캘린더
    const company = screen.getByText("기업분석 시작").closest('[role="link"]');
    expect(company).toHaveAttribute("aria-disabled", "true");
    const calendar = screen.getByText("캘린더 보기").closest('[role="link"]');
    expect(calendar).toHaveAttribute("aria-disabled", "true");
    // "준비중" 배지 2개
    expect(screen.getAllByText("준비중")).toHaveLength(2);
  });
});
```

(`screen.getByText("7")` 가 다른 곳의 "7" 과 겹칠 위험이 있으니, 실제 실행 시 만약 multiple matches 에러 나면 `within(card).getByText("7")` 같은 scope 한정으로 조정. 우선 단순 형태로 시도.)

- [ ] **Step 2: 테스트 실행 — 결과 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npx vitest run src/components/dashboard/__tests__/dashboard-content.test.tsx 2>&1 | tail -25
```

Expected: 5 케이스 모두 PASS. 실패 시 출력의 received vs expected 확인:
- `getByText("7")` multiple matches → `within` 또는 정규식·옵션으로 scope 좁히기
- `closest("a")` 가 null → 컴포넌트가 `<a>` 가 아닌 `<Link>` 의 자식을 다른 구조로 렌더하는 경우. 위 mock 처리(next/link → `<a>`) 가 적용되는지 확인
- `getByRole("status")` 가 매치 안 됨 → HomeBanner 의 `role="status"` 확인

- [ ] **Step 3: 만약 multiple matches 등 픽스가 필요했다면 한번 더 실행 — 5 PASS 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npx vitest run src/components/dashboard/__tests__/dashboard-content.test.tsx 2>&1 | tail -10
```

Expected: 5 PASS.

- [ ] **Step 4: 전체 회귀**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm test 2>&1 | tail -12
```

Expected: 기존 30 + greeting 2 + dashboard-content 5 = **37 PASS**.

- [ ] **Step 5: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/dashboard/__tests__/dashboard-content.test.tsx
git commit -m "test(dashboard): DashboardContent 통합 Vitest 5케이스

- 온보딩 완료자: 닉네임 + 부제 태그 3 + HomeBanner 미표시
- 온보딩 미완료자: 닉네임 + 태그 숨김 + HomeBanner 표시
- 3-card KPI 렌더 (72/14/7)
- UpcomingPanel 일정 4건 회사·시간
- Shortcuts — 2 작동 (a href) + 2 aria-disabled + 준비중 배지 2개"
```

---

## Task 10: 종단 점검 + 푸시 + PR

- [ ] **Step 1: 전체 회귀 검증**

```bash
cd /Users/sungjiwon/claude/2chi_v1/backend
./gradlew test 2>&1 | tail -3
cd ../frontend
npm test 2>&1 | tail -10
npm run build 2>&1 | tail -10
```

Expected:
- 백엔드: 변경 없음, 전부 PASS
- 프론트: 37 PASS (30 + 7)
- BUILD SUCCESSFUL — `/` 라우트가 dashboard.css + dashboard 컴포넌트 트리 포함

- [ ] **Step 2: dev server e2e 점검 (수동)**

```bash
cd /Users/sungjiwon/claude/2chi_v1/backend
./gradlew bootRun > /tmp/be.log 2>&1 &
BE=$!
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm run dev > /tmp/fe.log 2>&1 &
FE=$!

sleep 20

# 라우트 살아있음 확인
for p in / /me /applications /cover-letter /jobs /signup /login /onboarding /terms /privacy; do
  curl -sf -o /dev/null "http://localhost:3000$p" && echo "OK $p" || echo "FAIL $p"
done

kill $BE $FE 2>/dev/null
wait 2>/dev/null
```

Expected: 모든 경로 OK.

브라우저에서 직접 확인 (수동):
- 로그인 → `/` 도달 → 대시보드 풀 렌더 (인사·KPI·일정·바로가기·TodayQuote)
- 비로그인 상태로 `/` 접근 → `/login?from=%2F` 리다이렉트
- 바로가기 "자소서 작성" 클릭 → `/cover-letter` 도달
- 바로가기 "기업분석 시작" 클릭 → 무반응 + 시각적 disabled 확인

- [ ] **Step 3: 푸시**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git push -u origin feat/5.3-dashboard
```

- [ ] **Step 4: CI 대기**

```bash
sleep 30
gh run list --branch feat/5.3-dashboard --limit 1
gh run watch --exit-status
```

Expected: backend + frontend ✅.

- [ ] **Step 5: PR 생성**

```bash
gh pr create --base develop --head feat/5.3-dashboard \
  --title "feat: 5.3 Dashboard — 인사 헤더 + 3-card KPI + 일정 + 바로가기 (Mock)" \
  --body "$(cat <<'EOF'
## 요약
- 로그인 후 첫 화면(대시보드 홈) 구현 — 시안 \`design_system/pages/dashboard.{html,css,jsx}\` 1:1 매핑
- 데이터 정책: \`/me\` 만 실데이터, KPI/일정/태그는 \`lib/mock/dashboard.ts\` Mock 상수
- 위젯: Greeting / KpiCompleteness / KpiCoverLetters / KpiInProgress / UpcomingPanel / TodayQuote / Shortcuts
- HomeBanner 는 그대로 (.dash-main 안에서 온보딩 미완료자에게 표시)
- CSS: \`design_system/pages/dashboard.css\` 를 \`(app)/dashboard.css\` 로 복사·변환
  - .dash-nav* (TopNav 영역) 삭제 — 5.0 TopNav 와 충돌 방지
  - body.dash-body → .dash-page 래퍼
- 테스트: greeting 2 + dashboard-content 5 = **신규 7**, 전체 프론트 37

## 제외 (다음 브랜치)
- 매칭 분석 패널 — 5.10/5.11 AI 도메인
- 기업분석/캘린더 라우트 추가 — 각 도메인 브랜치 (5.10, 5.8)
- 실데이터 위젯 — 5.4 (이력서), 5.6 (자소서), 5.7 (지원), 5.8 (일정)
- 반응형 (모바일) — 1280+ only

## 시안 참조
- \`design_system/uploads/2chi_v1_wireframes_v0.1.html\` §2 대시보드 (홈)
- \`design_system/pages/dashboard.html\` / \`dashboard.css\` / \`dashboard.jsx\`
- 설계 문서: [docs/superpowers/specs/2026-05-20-feat-5.3-dashboard-design.md](docs/superpowers/specs/2026-05-20-feat-5.3-dashboard-design.md)

## 검증
- 백엔드: 변경 없음
- 프론트: 37 Vitest PASS, build 통과
- 로그인 → \`/\` 대시보드 풀 렌더 (수동 확인)
- 바로가기 disabled 2개 클릭 무반응 + "준비중" 배지

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

PR URL 출력 확인.

---

## 자체 점검 (작성자가 작성 후 1회 빠르게 확인)

- [ ] 모든 step 에 실제 코드 또는 명령어 포함 (placeholder 없음)
- [ ] 파일 경로는 절대경로 또는 정확한 상대경로 (`(app)` 같이 괄호 포함된 경로는 zsh 에서 escape)
- [ ] TDD: greeting / dashboard-content 는 테스트 먼저, 그 외 위젯은 빌드·dev server 시각 확인으로 검증
- [ ] 각 Task 끝에 commit 단계 — 작은 단위로 빈번한 커밋
- [ ] Spec §1~§11 모든 결정 사항 → Task 1~10 에 매핑됨
  - §1 범위·제외 ✓
  - §2 접근법 (Server page + Client content) → Task 8
  - §3 파일 구조 ✓ 위 파일 맵과 동일
  - §4 데이터 흐름 → Task 8 의 dashboard-content
  - §5 Greeting 매핑 (날짜 포맷·showTags) → Task 4
  - §6 KPI/UpcomingPanel/Shortcuts → Task 5/6/7
  - §7 CSS 변환 4건 → Task 1 Step 2/3
  - §8 icons.tsx 12종 → Task 3
  - §9 테스트 7케이스 → Task 4 + Task 9
  - §10 비기능 → 의도적 미커버 (런타임 변경 없음)
  - §11 후속 브랜치 → spec 에 명시 (플랜 외)
