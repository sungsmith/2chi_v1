# feat/5.0-app-shell Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 인증된 사용자 공통 레이아웃(TopNav + 인증 가드) 도입. 라우트 그룹 `(public)/`·`(app)/` 분리. 5개 메뉴와 4개 placeholder 페이지 생성.

**Architecture:** Next.js App Router 의 route group 으로 비인증·인증 영역 분리. `(app)/layout.tsx` 가 비로그인 시 `/login?from=...` 리다이렉트 + TopNav 렌더. TopNav 는 NavMenu(5개) + NavIconButton(검색·알림 placeholder) + ProfileMenu(드롭다운: 마이페이지·로그아웃) 으로 구성. LoginForm 은 `from` 쿼리로 원래 페이지 복귀.

**Tech Stack:** Next.js 16 / React 19 / TypeScript / Vitest + Testing Library

---

## 작업 브랜치

`feat/5.0-app-shell` (설계 문서 `81cb6d9` 포함).

---

## Task 1: 로고 파일 복사 + 라우트 그룹 재배치

**Files:**
- Copy: `design_system/assets/logo.svg` → `frontend/public/logo.svg`
- Move: `frontend/src/app/{signup,login,onboarding,terms,privacy}/` → `frontend/src/app/(public)/{signup,login,onboarding,terms,privacy}/`
- Create: `frontend/src/app/(app)/` 디렉토리

- [ ] **Step 1: 로고 복사**

```bash
cd /Users/sungjiwon/claude/2chi_v1
cp design_system/assets/logo.svg frontend/public/logo.svg
ls -la frontend/public/logo.svg
```

Expected: 파일 존재.

- [ ] **Step 2: (public) 그룹으로 5개 라우트 이동**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend/src/app
mkdir -p '(public)'
git mv signup '(public)/signup'
git mv login '(public)/login'
git mv onboarding '(public)/onboarding'
git mv terms '(public)/terms'
git mv privacy '(public)/privacy'
```

Verify: `ls '(public)/'` 에 5 디렉토리. 기존 `signup/`, `login/`, `onboarding/`, `terms/`, `privacy/` 가 root app/ 에 더 이상 없음.

- [ ] **Step 3: (app) 그룹 디렉토리 생성**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend/src/app
mkdir -p '(app)'
```

- [ ] **Step 4: 기존 app/page.tsx 와 globals.css 확인**

`app/page.tsx` 와 `app/layout.tsx`, `app/globals.css` 는 그대로 root 에 유지. page.tsx 는 다음 Task 에서 `(app)/page.tsx` 로 이동.

- [ ] **Step 5: 빌드 확인 (page 이동 전이지만 라우트 그룹 자체 동작 확인)**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm run build
```

Expected: BUILD SUCCESSFUL. 기존 라우트(`/signup`, `/login`, `/onboarding`, `/terms`, `/privacy`) 와 `/` 모두 동일 URL 로 빌드됨.

- [ ] **Step 6: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/public/logo.svg frontend/src/app
git status --short
```

Verify: 5개 디렉토리 rename (`R` 상태) + logo.svg 추가.

```bash
git commit -m "chore(frontend): 로고 복사 + 라우트 (public) 그룹으로 이동"
```

---

## Task 2: NavIconButton + NavMenu 컴포넌트

**Files:**
- Create: `frontend/src/components/app-shell/nav-icon-button.tsx`
- Create: `frontend/src/components/app-shell/nav-menu.tsx`

- [ ] **Step 1: NavIconButton 작성**

`frontend/src/components/app-shell/nav-icon-button.tsx`:

```tsx
"use client";

import { ReactNode } from "react";

type Props = {
  ariaLabel: string;
  onClick: () => void;
  children: ReactNode;
};

export function NavIconButton({ ariaLabel, onClick, children }: Props) {
  return (
    <button
      type="button"
      aria-label={ariaLabel}
      onClick={onClick}
      style={{
        width: 36,
        height: 36,
        display: "inline-flex",
        alignItems: "center",
        justifyContent: "center",
        background: "transparent",
        border: "none",
        borderRadius: "var(--radius-md)",
        color: "var(--color-text-secondary)",
        cursor: "pointer",
      }}
    >
      {children}
    </button>
  );
}
```

- [ ] **Step 2: NavMenu 작성**

`frontend/src/components/app-shell/nav-menu.tsx`:

```tsx
"use client";

import Link from "next/link";
import { usePathname } from "next/navigation";

const MENUS = [
  { href: "/", label: "대시보드" },
  { href: "/me", label: "내 정보" },
  { href: "/applications", label: "지원 현황" },
  { href: "/cover-letter", label: "이직 / 취업" },
  { href: "/jobs", label: "기업" },
] as const;

export function NavMenu() {
  const pathname = usePathname();

  return (
    <nav style={{ display: "flex", gap: "var(--space-5)" }}>
      {MENUS.map((m) => {
        const active = m.href === "/" ? pathname === "/" : pathname.startsWith(m.href);
        return (
          <Link
            key={m.href}
            href={m.href}
            style={{
              position: "relative",
              padding: "var(--space-2) 0",
              fontSize: "var(--fs-body-sm)",
              fontWeight: 600,
              color: active ? "var(--color-text-brand)" : "var(--color-text-secondary)",
              textDecoration: "none",
            }}
            aria-current={active ? "page" : undefined}
          >
            {m.label}
            {active && (
              <span
                aria-hidden
                style={{
                  position: "absolute",
                  left: 0,
                  right: 0,
                  bottom: -1,
                  height: 2,
                  background: "var(--color-primary-500)",
                  borderRadius: 1,
                }}
              />
            )}
          </Link>
        );
      })}
    </nav>
  );
}
```

- [ ] **Step 3: 빌드 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm run build
```

Expected: BUILD SUCCESSFUL. NavMenu, NavIconButton 은 아직 어떤 페이지에서도 사용 안 되지만 컴파일 통과해야 함.

- [ ] **Step 4: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/app-shell
git commit -m "feat(app-shell): NavMenu + NavIconButton 컴포넌트"
```

---

## Task 3: ProfileMenu (드롭다운)

**Files:**
- Create: `frontend/src/components/app-shell/profile-menu.tsx`

- [ ] **Step 1: ProfileMenu 작성**

`frontend/src/components/app-shell/profile-menu.tsx`:

```tsx
"use client";

import { useState, useRef, useEffect } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import { useAuth } from "@/contexts/auth-context";

export function ProfileMenu() {
  const { user, logout } = useAuth();
  const router = useRouter();
  const [open, setOpen] = useState(false);
  const ref = useRef<HTMLDivElement>(null);

  useEffect(() => {
    function onClickOutside(e: MouseEvent) {
      if (ref.current && !ref.current.contains(e.target as Node)) setOpen(false);
    }
    document.addEventListener("mousedown", onClickOutside);
    return () => document.removeEventListener("mousedown", onClickOutside);
  }, []);

  if (!user) return null;

  async function handleLogout() {
    await logout();
    setOpen(false);
    router.push("/login");
  }

  const initial = user.nickname.charAt(0);

  return (
    <div ref={ref} style={{ position: "relative" }}>
      <button
        type="button"
        onClick={() => setOpen((v) => !v)}
        aria-haspopup="menu"
        aria-expanded={open}
        style={{
          display: "flex",
          alignItems: "center",
          gap: "var(--space-2)",
          padding: "var(--space-2) var(--space-3)",
          background: "transparent",
          border: "none",
          borderRadius: "var(--radius-full)",
          cursor: "pointer",
        }}
      >
        <span
          aria-hidden
          style={{
            width: 28,
            height: 28,
            borderRadius: "var(--radius-full)",
            background: "var(--color-primary-100)",
            color: "var(--color-text-brand)",
            display: "inline-flex",
            alignItems: "center",
            justifyContent: "center",
            fontSize: "var(--fs-body-sm)",
            fontWeight: 700,
          }}
        >
          {initial}
        </span>
        <span style={{ fontSize: "var(--fs-body-sm)", color: "var(--color-text-primary)" }}>
          {user.nickname}
        </span>
      </button>

      {open && (
        <div
          role="menu"
          style={{
            position: "absolute",
            right: 0,
            top: "calc(100% + 4px)",
            minWidth: 180,
            background: "var(--color-surface-default)",
            border: "1px solid var(--color-border-default)",
            borderRadius: "var(--radius-md)",
            boxShadow: "var(--shadow-floating)",
            padding: "var(--space-2)",
            zIndex: 60,
          }}
        >
          <Link
            href="/me"
            role="menuitem"
            onClick={() => setOpen(false)}
            style={menuItemStyle}
          >
            마이페이지
          </Link>
          <button
            type="button"
            role="menuitem"
            onClick={handleLogout}
            style={{
              ...menuItemStyle,
              width: "100%",
              textAlign: "left",
              border: "none",
              background: "transparent",
              cursor: "pointer",
            }}
          >
            로그아웃
          </button>
        </div>
      )}
    </div>
  );
}

const menuItemStyle: React.CSSProperties = {
  display: "block",
  padding: "var(--space-2) var(--space-3)",
  fontSize: "var(--fs-body-sm)",
  color: "var(--color-text-primary)",
  borderRadius: "var(--radius-sm)",
  textDecoration: "none",
};
```

- [ ] **Step 2: 빌드 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm run build
```

Expected: BUILD SUCCESSFUL.

- [ ] **Step 3: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/app-shell/profile-menu.tsx
git commit -m "feat(app-shell): ProfileMenu 드롭다운 (마이페이지/로그아웃)"
```

---

## Task 4: TopNav (전체 조립)

**Files:**
- Create: `frontend/src/components/app-shell/top-nav.tsx`

- [ ] **Step 1: TopNav 작성**

`frontend/src/components/app-shell/top-nav.tsx`:

```tsx
"use client";

import Link from "next/link";
import { NavMenu } from "./nav-menu";
import { NavIconButton } from "./nav-icon-button";
import { ProfileMenu } from "./profile-menu";

export function TopNav() {
  return (
    <header
      style={{
        position: "sticky",
        top: 0,
        zIndex: 50,
        height: 64,
        background: "var(--color-surface-default)",
        borderBottom: "1px solid var(--color-border-subtle)",
      }}
    >
      <div
        style={{
          maxWidth: 1280,
          margin: "0 auto",
          padding: "0 var(--space-6)",
          height: "100%",
          display: "flex",
          alignItems: "center",
          gap: "var(--space-8)",
        }}
      >
        <Link
          href="/"
          aria-label="이취 (2chi)"
          style={{ display: "flex", alignItems: "center" }}
        >
          <img src="/logo.svg" alt="이취" style={{ height: 32 }} />
        </Link>

        <NavMenu />

        <div
          style={{
            marginLeft: "auto",
            display: "flex",
            alignItems: "center",
            gap: "var(--space-2)",
          }}
        >
          <NavIconButton
            ariaLabel="검색"
            onClick={() => alert("검색 기능은 곧 제공됩니다.")}
          >
            <SearchIcon />
          </NavIconButton>
          <NavIconButton
            ariaLabel="알림"
            onClick={() => alert("알림 기능은 곧 제공됩니다.")}
          >
            <BellIcon />
          </NavIconButton>
          <ProfileMenu />
        </div>
      </div>
    </header>
  );
}

function SearchIcon() {
  return (
    <svg
      width={18}
      height={18}
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth={1.8}
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <circle cx={11} cy={11} r={7} />
      <path d="m21 21-4.3-4.3" />
    </svg>
  );
}

function BellIcon() {
  return (
    <svg
      width={18}
      height={18}
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth={1.8}
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <path d="M6 8a6 6 0 0 1 12 0c0 7 3 9 3 9H3s3-2 3-9" />
      <path d="M10 21a2 2 0 0 0 4 0" />
    </svg>
  );
}
```

- [ ] **Step 2: 빌드 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm run build
```

Expected: BUILD SUCCESSFUL.

- [ ] **Step 3: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/app-shell/top-nav.tsx
git commit -m "feat(app-shell): TopNav 조립 (로고/메뉴/검색·알림/프로필)"
```

---

## Task 5: AppShellLayout + 홈 페이지 이동

**Files:**
- Create: `frontend/src/app/(app)/layout.tsx`
- Move: `frontend/src/app/page.tsx` → `frontend/src/app/(app)/page.tsx`

- [ ] **Step 1: (app)/layout.tsx 작성**

`frontend/src/app/(app)/layout.tsx`:

```tsx
"use client";

import { useEffect } from "react";
import { useRouter, usePathname } from "next/navigation";
import { useAuth } from "@/contexts/auth-context";
import { TopNav } from "@/components/app-shell/top-nav";

export default function AppLayout({ children }: { children: React.ReactNode }) {
  const { user, initialized } = useAuth();
  const router = useRouter();
  const pathname = usePathname();

  useEffect(() => {
    if (!initialized) return;
    if (!user) router.replace(`/login?from=${encodeURIComponent(pathname)}`);
  }, [initialized, user, router, pathname]);

  if (!initialized) return null;
  if (!user) return null;

  return (
    <>
      <TopNav />
      <main
        style={{
          background: "var(--color-bg)",
          minHeight: "calc(100vh - 64px)",
        }}
      >
        {children}
      </main>
    </>
  );
}
```

- [ ] **Step 2: 홈 page.tsx 를 (app) 그룹으로 이동**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend/src/app
git mv page.tsx '(app)/page.tsx'
```

기존 `page.tsx` 내용은 그대로 유지. 안에서 `import { HomeBanner } from "@/components/home/home-banner";` 도 그대로.

- [ ] **Step 3: 빌드 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm run build
```

Expected: BUILD SUCCESSFUL. `/` 라우트가 `(app)/page.tsx` 에서 렌더되고 `(app)/layout.tsx` 로 감싸짐.

- [ ] **Step 4: 기존 SignupForm/LoginForm/Onboarding 테스트 회귀 확인**

```bash
npm test
```

Expected: PASS. AppShellLayout 변경은 컴포넌트 테스트에 영향 없음.

- [ ] **Step 5: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/app
git commit -m "feat(app-shell): (app)/layout.tsx 인증 가드 + 홈 페이지 이동"
```

---

## Task 6: placeholder 페이지 4개

**Files:**
- Create: `frontend/src/app/(app)/me/page.tsx`
- Create: `frontend/src/app/(app)/applications/page.tsx`
- Create: `frontend/src/app/(app)/cover-letter/page.tsx`
- Create: `frontend/src/app/(app)/jobs/page.tsx`

- [ ] **Step 1: /me placeholder**

`frontend/src/app/(app)/me/page.tsx`:

```tsx
export const metadata = {
  title: "내 정보 · 2chi",
};

export default function MePage() {
  return (
    <section
      style={{
        maxWidth: 720,
        margin: "0 auto",
        padding: "var(--space-12) var(--space-6)",
      }}
    >
      <h1
        style={{
          fontFamily: "var(--font-family-hand)",
          fontSize: "var(--fs-page-title)",
        }}
      >
        내 정보
      </h1>
      <p
        style={{
          color: "var(--color-text-secondary)",
          marginTop: "var(--space-4)",
        }}
      >
        이 기능은 다음 단계에 구현됩니다.
      </p>
    </section>
  );
}
```

- [ ] **Step 2: /applications placeholder**

`frontend/src/app/(app)/applications/page.tsx`:

```tsx
export const metadata = {
  title: "지원 현황 · 2chi",
};

export default function ApplicationsPage() {
  return (
    <section
      style={{
        maxWidth: 720,
        margin: "0 auto",
        padding: "var(--space-12) var(--space-6)",
      }}
    >
      <h1
        style={{
          fontFamily: "var(--font-family-hand)",
          fontSize: "var(--fs-page-title)",
        }}
      >
        지원 현황
      </h1>
      <p
        style={{
          color: "var(--color-text-secondary)",
          marginTop: "var(--space-4)",
        }}
      >
        이 기능은 다음 단계에 구현됩니다.
      </p>
    </section>
  );
}
```

- [ ] **Step 3: /cover-letter placeholder**

`frontend/src/app/(app)/cover-letter/page.tsx`:

```tsx
export const metadata = {
  title: "이직 / 취업 · 2chi",
};

export default function CoverLetterPage() {
  return (
    <section
      style={{
        maxWidth: 720,
        margin: "0 auto",
        padding: "var(--space-12) var(--space-6)",
      }}
    >
      <h1
        style={{
          fontFamily: "var(--font-family-hand)",
          fontSize: "var(--fs-page-title)",
        }}
      >
        이직 / 취업
      </h1>
      <p
        style={{
          color: "var(--color-text-secondary)",
          marginTop: "var(--space-4)",
        }}
      >
        이 기능은 다음 단계에 구현됩니다.
      </p>
    </section>
  );
}
```

- [ ] **Step 4: /jobs placeholder**

`frontend/src/app/(app)/jobs/page.tsx`:

```tsx
export const metadata = {
  title: "기업 · 2chi",
};

export default function JobsPage() {
  return (
    <section
      style={{
        maxWidth: 720,
        margin: "0 auto",
        padding: "var(--space-12) var(--space-6)",
      }}
    >
      <h1
        style={{
          fontFamily: "var(--font-family-hand)",
          fontSize: "var(--fs-page-title)",
        }}
      >
        기업
      </h1>
      <p
        style={{
          color: "var(--color-text-secondary)",
          marginTop: "var(--space-4)",
        }}
      >
        이 기능은 다음 단계에 구현됩니다.
      </p>
    </section>
  );
}
```

- [ ] **Step 5: 빌드 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm run build
```

Expected: BUILD SUCCESSFUL. 4개 라우트 추가:
- /me
- /applications
- /cover-letter
- /jobs

- [ ] **Step 6: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/app/'(app)'
git commit -m "feat(app-shell): /me, /applications, /cover-letter, /jobs placeholder 페이지"
```

---

## Task 7: LoginForm — from 쿼리 분기

**Files:**
- Modify: `frontend/src/components/login/login-form.tsx`
- Modify: `frontend/src/components/login/__tests__/login-form.test.tsx`

- [ ] **Step 1: LoginForm 수정 — from 파라미터 처리**

`frontend/src/components/login/login-form.tsx` 의 import 영역에 `useSearchParams` 추가:

```tsx
import { useRouter, useSearchParams } from "next/navigation";
```

컴포넌트 함수 본문 상단에 추가:

```tsx
  const searchParams = useSearchParams();
```

`handleSubmit` 의 성공 경로를 다음으로 변경:

기존:
```ts
      const user = await login(email, password);
      router.push(user.onboardingCompleted ? "/" : "/onboarding");
```

새:
```ts
      const u = await login(email, password);
      const from = searchParams.get("from");
      const safeFrom = from && from.startsWith("/") ? from : null;
      const dest = u.onboardingCompleted ? (safeFrom ?? "/") : "/onboarding";
      router.push(dest);
```

- [ ] **Step 2: 기존 테스트 회귀 확인**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm test
```

Expected: 기존 login-form 5 케이스 PASS (from 미존재 시 기존 분기 동작 — `safeFrom = null` → onboardingCompleted 분기).

만약 `useSearchParams` 모킹 누락으로 실패하면 다음 mock 추가가 필요:

```ts
vi.mock("next/navigation", () => ({
  useRouter: () => ({ push: pushMock }),
  useSearchParams: () => ({ get: () => null }),
}));
```

- [ ] **Step 3: from 분기 테스트 추가**

`login-form.test.tsx` 의 기존 `vi.mock("next/navigation", ...)` 의 mock 을 다음과 같이 변경 (현재 useSearchParams 누락):

```ts
let mockSearchParams = new URLSearchParams();
vi.mock("next/navigation", () => ({
  useRouter: () => ({ push: pushMock }),
  useSearchParams: () => mockSearchParams,
}));
```

`beforeEach` 안에 추가:

```ts
mockSearchParams = new URLSearchParams();
```

그리고 클래스 끝에 새 케이스 추가:

```ts
  test("from=%2Fme + onboardingCompleted=true → /me 로 이동", async () => {
    mockSearchParams = new URLSearchParams("from=%2Fme");
    loginMock.mockResolvedValueOnce({
      userId: 1, email: "a@b.com", nickname: "alice", onboardingCompleted: true
    });
    const user = userEvent.setup();
    render(<LoginForm />);

    await user.type(screen.getByLabelText(/이메일/), "a@b.com");
    await user.type(screen.getByLabelText(/비밀번호/), "abc123");
    await user.click(screen.getByRole("button", { name: /로그인/ }));

    expect(pushMock).toHaveBeenCalledWith("/me");
  });

  test("from=javascript:alert + onboardingCompleted=true → / 로 안전 fallback", async () => {
    mockSearchParams = new URLSearchParams("from=javascript%3Aalert(1)");
    loginMock.mockResolvedValueOnce({
      userId: 1, email: "a@b.com", nickname: "alice", onboardingCompleted: true
    });
    const user = userEvent.setup();
    render(<LoginForm />);

    await user.type(screen.getByLabelText(/이메일/), "a@b.com");
    await user.type(screen.getByLabelText(/비밀번호/), "abc123");
    await user.click(screen.getByRole("button", { name: /로그인/ }));

    expect(pushMock).toHaveBeenCalledWith("/");
  });
```

`URLSearchParams` 는 mock 으로도 `.get()` 메서드 동작 — `URLSearchParams("from=javascript%3Aalert(1)").get("from")` → `"javascript:alert(1)"` (디코드됨). `startsWith("/")` 가 false 라 fallback.

- [ ] **Step 4: 테스트 실행**

```bash
npm test
```

Expected: 모든 frontend 테스트 PASS (기존 + 신규 2).

- [ ] **Step 5: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/login
git commit -m "feat(auth): LoginForm — from 쿼리로 원래 페이지 복귀 + 안전 검증"
```

---

## Task 8: TopNav 컴포넌트 테스트

**Files:**
- Create: `frontend/src/components/app-shell/__tests__/top-nav.test.tsx`

- [ ] **Step 1: TopNav 테스트 작성**

`frontend/src/components/app-shell/__tests__/top-nav.test.tsx`:

```tsx
import { describe, expect, test, vi, beforeEach, afterEach } from "vitest";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { TopNav } from "../top-nav";

const pushMock = vi.fn();
let mockPathname = "/";

vi.mock("next/navigation", () => ({
  useRouter: () => ({ push: pushMock }),
  usePathname: () => mockPathname,
}));

const logoutMock = vi.fn();
let authMock = {
  user: { userId: 1, email: "a@b.com", nickname: "alice", onboardingCompleted: true },
  initialized: true,
  login: vi.fn(),
  logout: logoutMock,
  refreshUser: vi.fn(),
};
vi.mock("@/contexts/auth-context", () => ({
  useAuth: () => authMock,
}));

beforeEach(() => {
  pushMock.mockReset();
  logoutMock.mockReset();
  mockPathname = "/";
  authMock = {
    user: { userId: 1, email: "a@b.com", nickname: "alice", onboardingCompleted: true },
    initialized: true,
    login: vi.fn(),
    logout: logoutMock,
    refreshUser: vi.fn(),
  };
  vi.stubGlobal("alert", vi.fn());
});

afterEach(() => {
  vi.unstubAllGlobals();
});

describe("TopNav", () => {
  test("로그인 사용자 닉네임 표시", () => {
    render(<TopNav />);
    expect(screen.getByText("alice")).toBeInTheDocument();
  });

  test("5개 메뉴 렌더 + 현재 경로(/) 의 '대시보드' active", () => {
    mockPathname = "/";
    render(<TopNav />);

    const dashboard = screen.getByRole("link", { name: /대시보드/ });
    expect(dashboard).toHaveAttribute("aria-current", "page");

    const me = screen.getByRole("link", { name: /내 정보/ });
    expect(me).not.toHaveAttribute("aria-current");
  });

  test("pathname=/me 시 '내 정보' active", () => {
    mockPathname = "/me";
    render(<TopNav />);

    const me = screen.getByRole("link", { name: /내 정보/ });
    expect(me).toHaveAttribute("aria-current", "page");

    const dashboard = screen.getByRole("link", { name: /대시보드/ });
    expect(dashboard).not.toHaveAttribute("aria-current");
  });

  test("프로필 클릭 → 드롭다운 열림, 로그아웃 클릭 → logout + /login 이동", async () => {
    logoutMock.mockResolvedValueOnce(undefined);
    const user = userEvent.setup();
    render(<TopNav />);

    expect(screen.queryByRole("menuitem", { name: /로그아웃/ })).not.toBeInTheDocument();

    await user.click(screen.getByRole("button", { name: /alice/ }));

    expect(screen.getByRole("menuitem", { name: /마이페이지/ })).toBeInTheDocument();

    await user.click(screen.getByRole("menuitem", { name: /로그아웃/ }));

    expect(logoutMock).toHaveBeenCalledTimes(1);
    expect(pushMock).toHaveBeenCalledWith("/login");
  });

  test("검색 버튼 클릭 → alert 호출", async () => {
    const user = userEvent.setup();
    render(<TopNav />);

    await user.click(screen.getByRole("button", { name: /검색/ }));

    expect(window.alert).toHaveBeenCalledWith("검색 기능은 곧 제공됩니다.");
  });

  test("알림 버튼 클릭 → alert 호출", async () => {
    const user = userEvent.setup();
    render(<TopNav />);

    await user.click(screen.getByRole("button", { name: /알림/ }));

    expect(window.alert).toHaveBeenCalledWith("알림 기능은 곧 제공됩니다.");
  });

  test("user=null 시 ProfileMenu 미렌더 (닉네임 부재)", () => {
    authMock = { ...authMock, user: null };
    render(<TopNav />);
    expect(screen.queryByText("alice")).not.toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 테스트 실행**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm test
```

Expected: 모든 테스트 PASS (기존 + 새로 추가된 7 케이스).

흔한 이슈:
- `getByRole("button", { name: /alice/ })` 가 매칭 안 되면 — ProfileMenu 의 button 안에 닉네임이 텍스트로 들어가는지 확인. accessible name 이 다른 텍스트(예: "소 alice") 와 합쳐졌을 수 있음. 그 경우 `getAllByRole("button").find(...)` 같은 보정 또는 `aria-label` 추가
- jsdom 의 useEffect (외부 클릭 리스너) 가 트리거 안 됨 — `user.click(외부 요소)` 로 외부 클릭 시뮬레이션 필요. 본 테스트에서는 외부 클릭 케이스 안 다룸

- [ ] **Step 3: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/components/app-shell/__tests__
git commit -m "test(app-shell): TopNav Vitest 7 케이스"
```

---

## Task 9: (app)/layout 인증 가드 테스트

**Files:**
- Create: `frontend/src/app/(app)/__tests__/layout.test.tsx`

- [ ] **Step 1: 레이아웃 가드 테스트**

`frontend/src/app/(app)/__tests__/layout.test.tsx`:

```tsx
import { describe, expect, test, vi, beforeEach } from "vitest";
import { render, screen } from "@testing-library/react";
import AppLayout from "../layout";

const replaceMock = vi.fn();
let mockPathname = "/me";

vi.mock("next/navigation", () => ({
  useRouter: () => ({ replace: replaceMock, push: vi.fn() }),
  usePathname: () => mockPathname,
}));

let authMock = {
  user: { userId: 1, email: "a@b.com", nickname: "alice", onboardingCompleted: true },
  initialized: true,
  login: vi.fn(),
  logout: vi.fn(),
  refreshUser: vi.fn(),
};
vi.mock("@/contexts/auth-context", () => ({
  useAuth: () => authMock,
}));

beforeEach(() => {
  replaceMock.mockReset();
  mockPathname = "/me";
  authMock = {
    user: { userId: 1, email: "a@b.com", nickname: "alice", onboardingCompleted: true },
    initialized: true,
    login: vi.fn(),
    logout: vi.fn(),
    refreshUser: vi.fn(),
  };
});

describe("AppLayout", () => {
  test("인증된 사용자 → children 렌더", () => {
    render(<AppLayout><div data-testid="child">hi</div></AppLayout>);
    expect(screen.getByTestId("child")).toBeInTheDocument();
  });

  test("비로그인 사용자 → /login?from=... 리다이렉트", () => {
    authMock = { ...authMock, user: null };
    mockPathname = "/applications";
    render(<AppLayout><div>hi</div></AppLayout>);
    expect(replaceMock).toHaveBeenCalledWith("/login?from=%2Fapplications");
  });

  test("초기화 전(initialized=false) → 깜빡임 없이 null", () => {
    authMock = { ...authMock, initialized: false, user: null };
    const { container } = render(<AppLayout><div>hi</div></AppLayout>);
    expect(container.firstChild).toBeNull();
    expect(replaceMock).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 테스트 실행**

```bash
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm test
```

Expected: PASS.

- [ ] **Step 3: 커밋**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git add frontend/src/app/'(app)'/__tests__
git commit -m "test(app-shell): (app)/layout 인증 가드 Vitest 3 케이스"
```

---

## Task 10: 종단 점검 + 푸시 + PR

- [ ] **Step 1: 전체 회귀 검증**

```bash
cd /Users/sungjiwon/claude/2chi_v1/backend
./gradlew test 2>&1 | tail -3
cd ../frontend
npm test 2>&1 | tail -8
npm run build 2>&1 | tail -10
```

Expected:
- 백엔드 모든 테스트 PASS (변경 없음)
- 프론트 18 + TopNav 7 + Layout 3 + LoginForm +2 = 30 PASS
- BUILD SUCCESSFUL — `/` (app/page.tsx 이동), `/me`, `/applications`, `/cover-letter`, `/jobs`, `/signup`, `/login`, `/onboarding`, `/terms`, `/privacy` 라우트 표시

- [ ] **Step 2: dev server e2e 점검 (수동)**

```bash
cd /Users/sungjiwon/claude/2chi_v1/backend
./gradlew bootRun > /tmp/be.log 2>&1 &
BE=$!
cd /Users/sungjiwon/claude/2chi_v1/frontend
npm run dev > /tmp/fe.log 2>&1 &
FE=$!

sleep 18

# 비로그인 /me → /login?from=%2Fme 리다이렉트 (HTTP 200 + 페이지 콘텐츠는 login)
curl -sf http://localhost:3000/me -o /dev/null && echo "ME_REACHED"

# 라우트 살아있음 확인
for p in / /me /applications /cover-letter /jobs /signup /login /onboarding /terms /privacy; do
  curl -sf -o /dev/null "http://localhost:3000$p" && echo "OK $p" || echo "FAIL $p"
done

kill $BE $FE 2>/dev/null
wait 2>/dev/null
```

Expected: 모든 경로 OK.

- [ ] **Step 3: 푸시**

```bash
cd /Users/sungjiwon/claude/2chi_v1
git push -u origin feat/5.0-app-shell
```

- [ ] **Step 4: CI 대기**

```bash
sleep 30
gh run list --branch feat/5.0-app-shell --limit 1
gh run watch --exit-status
```

Expected: backend + frontend ✅.

- [ ] **Step 5: PR 생성**

```bash
gh pr create --base develop --head feat/5.0-app-shell \
  --title "feat: 5.0 App Shell — TopNav + 인증 가드 + 라우트 그룹" \
  --body "$(cat <<'EOF'
## 요약
- 라우트 그룹화: `(public)/` (signup·login·terms·privacy·onboarding) / `(app)/` (인증 필요)
- AppShellLayout — TopNav 포함 + 비로그인 시 `/login?from=...` 리다이렉트
- TopNav — 로고 / 5개 메뉴(active 강조) / 검색·알림 placeholder / ProfileMenu
- ProfileMenu — 닉네임 아바타 + 마이페이지/로그아웃 드롭다운
- LoginForm — `from` 쿼리로 원래 페이지 복귀 (안전 검증 startsWith("/"))
- placeholder 페이지 4종: /me, /applications, /cover-letter, /jobs
- 로고 파일 frontend/public/logo.svg

## 제외 (다음 브랜치)
- 실제 대시보드 위젯 — feat/5.3-dashboard
- 알림 카운트 배지·목록 — feat/5.8-notifications
- 검색 기능 — 추후
- 마이페이지 실제 콘텐츠 — feat/5.1-auth-mypage
- 모바일 햄버거 메뉴 — v1 후속 또는 v2

## 검증
- [x] backend ./gradlew test (변경 없음, 기존 회귀 PASS)
- [x] frontend npm test (TopNav 7 + Layout 3 + LoginForm +2 = 12 신규)
- [x] CI ✅
- [x] e2e: 비로그인 /me 접근 → /login?from=%2Fme, 로그인 후 /me 복귀
EOF
)"
```

---

## 셀프 리뷰

**스펙 커버리지:**
- TopNav + 5메뉴 → Task 2 NavMenu ✅
- ProfileMenu (마이페이지·로그아웃) → Task 3 ✅
- 검색·알림 placeholder + alert → Task 4 ✅
- (app)/layout 인증 가드 + from 쿼리 → Task 5 + Task 7 ✅
- 라우트 그룹화 → Task 1 ✅
- placeholder 페이지 4종 → Task 6 ✅
- 로고 복사 → Task 1 ✅
- 테스트 — TopNav + Layout + LoginForm 회귀 → Task 7 + 8 + 9 ✅

**Identifier 일관성:**
- `useAuth` 의 user/initialized/login/logout/refreshUser 시그니처 일관 ✅
- 5개 메뉴 href = (`/`, `/me`, `/applications`, `/cover-letter`, `/jobs`) — TopNav · layout · placeholder · 테스트 모두 일치 ✅
- `aria-current="page"` active 강조 패턴 ✅
- 디자인 토큰 (`--color-*`, `--space-*`, `--radius-*`, `--fs-*`) 만 사용 ✅

**Placeholder:** 없음.
